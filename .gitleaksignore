# False positive: synthetic BPMN business-key literal in a unit test. The 'generic-api-key'
# rule matches the substring "key" in `business_key=` plus the literal's entropy; the value
# INAD-amh-CONTRATO-001 is a fabricated test fixture, not a credential.
# Scope: one historical commit on the t1.12-fernando-graph branch (PR #67). The literal is
# being rewritten to a low-entropy form pre-merge; drop this entry once that branch is deleted.
72c865eef650dfbb54b1f6ed873723f8e984cec7:tests/unit/agents/test_fernando.py:generic-api-key:584

# Same false-positive class, second occurrence: synthetic BPMN business-key literals on the
# t1.12-marina-graph branch (PR #71). Literals rewritten to low-entropy forms in commit b2290fc;
# these three fingerprints cover the immutable historical commits only (no force-push policy).
# All three removable once that branch is deleted post-merge. Durable fix (scoped .gitleaks.toml
# allowlist for the business-key shape) is in flight on t2.4-gitleaks-businesskey-allowlist.
7c4b87143ab68db6c234c4cf645858532a27ce35:docs/evidence-ledger.md:generic-api-key:63
9ef098ef79a80314b52feedc3d246e98ca9c6df1:tests/unit/agents/test_marina.py:generic-api-key:680
9ef098ef79a80314b52feedc3d246e98ca9c6df1:tests/unit/agents/test_marina.py:generic-api-key:691

# False positive, same class as above: synthetic BPMN business-key literal quoted from a live
# probe LOG LINE in the G1 gate-review document on the in-flight g1-gate-review branch
# (business_key=ESC-G1PROBE-fc8aa9ee, entropy 3.92, generic-api-key). Not covered by the
# T2.4 business-key allowlist ON PURPOSE: "G1PROBE" is not a lowercase tenant segment, so the
# literal fails the allowlist's anchored shape even inside the scoped docs/gates/ path —
# widening the shape for it would relax the R1-verified containment. Fingerprint-scoped
# (single commit:file:rule:line — cannot mask any other occurrence); drop this entry once the
# g1-gate-review branch is merged or deleted.
a636399779661e843b485ac2a05cbfa5bbd8e559:docs/gates/G1-gate-review.md:generic-api-key:175
# Third occurrence of the business-key FP class: the G1 gate record cites live BPMN business
# keys as gate evidence (docs/gates/G1-gate-review.md on the g1-gate-review branch, PR #78).
# Bridge for the immutable branch commit; the durable fix (extending the .gitleaks.toml
# business-key allowlist paths to docs/gates/*.md) is in flight on PR #74.

# Fourth occurrence of the business-key FP class: high-entropy synthetic business_key literals
# on the in-flight t1.12-andre-graph branch (author is rewriting them to low-entropy forms;
# these fingerprints cover the immutable pushed commit only). Durable shape allowlist: PR #74.
0d4bcbcf1a380db28195a1ff9ebdd757db749a4c:tests/unit/agents/test_andre.py:generic-api-key:1030
0d4bcbcf1a380db28195a1ff9ebdd757db749a4c:tests/unit/agents/test_andre.py:generic-api-key:1052

# G1 gate record: synthetic BPMN business key cited as gate evidence (squash-merge commit e8116ba;
# non-lowercase-tenant literal ESC-G1PROBE-fc8aa9ee cannot match the shape allowlist). Fingerprints
# cover immutable historical commits (e8116ba on origin/main + a1b044b from stashed working changes);
# durable fix (source rephrase of business_key=value to business_key value) removes the trip from
# current main content.
e8116ba82ebd06427bda89beca9aa1ec940e1a57:docs/gates/G1-gate-review.md:generic-api-key:175
a1b044b37c6551ab0de4c1b8315a5d95ea26245b:docs/gates/G1-gate-review.md:generic-api-key:175

# AND-02 (fleet/and02-metrics-egress-gate): mesma classe de falso positivo, quinta ocorrencia —
# `generic-api-key` casou um kwarg de fixture cujo NOME continha "key" e cujo valor era o literal
# do canario sintetico
# (o token "key" no identificador + entropia do literal) na suite de evals. O literal e' o
# CANARIO SINTETICO do proprio golden EVL-ANDRE-04: um nome de celula com CPF que o gate de
# egresso do Andre TEM de bloquear — nao e' credencial e nao pode ser trocado por um valor de
# baixa entropia sem destruir o que o eval prova.
# Correcao DURAVEL ja aplicada no conteudo atual da branch: o identificador foi renomeado para
# `hostile_cell_name`, que nao contem "key" e nao dispara a regra — o HEAD da branch esta limpo.
# Este fingerprint cobre APENAS o commit imutavel da prova fail-before (o par golden-vermelho ->
# fix-verde exigido pelo processo), que nao pode ser reescrito sob as regras de operacao do WP
# (proibidos rebase/reset/amend fora do HEAD). Escopo fingerprint (commit:file:rule:line):
# nao mascara nenhuma outra ocorrencia, nem a mesma linha em outro commit.
# REMOVER esta entrada assim que a branch for mesclada e apagada — ou imediatamente, se o dono
# preferir reescrever o commit 6615de0 (uma unica edicao de historia local torna isto inutil).
6615de0fc7fb66ed97b125380fd268c2a252bd37:tests/evals/test_dossier_adverse_evals.py:generic-api-key:249

# AND-02, entrada IRMA da de cima (mesma classe, mesma causa): o proprio COMENTARIO acima citava,
# em prosa, a atribuicao `<kwarg com "key">="<literal do canario>"` — e a regra generic-api-key
# nao distingue prosa de codigo. O texto ja foi reescrito para nao conter mais essa forma; este
# fingerprint cobre APENAS o commit imutavel que o introduziu. Mesma condicao de remocao da
# entrada acima.
d3c5ac141e0f43587bf6278579e43afe05bb9aa3:.gitleaksignore:generic-api-key:46
# Security run 34338638279: identical synthetic HumanCommand form identifiers in
# immutable offline unit-fixture commits reachable through published branches.
# These are form-selection data, not credentials. Exact commit:file:rule:line
# fingerprints preserve scanning of every other occurrence and all default rules.
92f98281696b31a0151d8ba0d48bdabe2d80a8be:tests/unit/gateway/human/test_receipt_recording_metadata.py:generic-api-key:42
1dd1afe304d62f8d4dd78304272ec64294112e70:tests/unit/portal/test_engine_profile.py:generic-api-key:42

# Independently reviewed immutable-history false positives: key type annotations
# and a synthetic BPMN fixture correlation identifier. No live credential values.
# Independently reviewed historical false positives: three Ed25519 dataclass
# type annotations with field(repr=False), and one synthetic BPMN businessKey.
# Exact commit:path:rule:line only; no credential literals or broad exclusions.
6770eb848bec667e3476d2032e2e13fac77d2118:src/maezo/gateway/human/credentials.py:generic-api-key:72
9253328bbae54d0a6b947db7bf792eef006c02c0:src/maezo/gateway/human/auth_transport.py:generic-api-key:64
262f004d3b976abaa1a2421a2f0e2d44dc35ddcf:src/maezo/gateway/human/read_credentials.py:generic-api-key:72
a71a14e15fde4515567d65564a019ce9a7c0f021:deploy/cibseven/secured/prepare_fixture.py:generic-api-key:349
d71dac0025c09df7d09cbbb822cb3ac0be9e0293:src/maezo/gateway/document_requests/composition.py:generic-api-key:260
206a53dd8c8d7a349c6d04db5631234c0b1c1252:src/maezo/gateway/staff_cases/publisher.py:generic-api-key:41
