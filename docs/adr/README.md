# ADRs — Maezo Healthcare Plan

Serie propria deste repo (0001+). O repo hospitalar `Omni-Saude/Maezo` tem serie
independente (001-020) usada apenas como referencia intelectual.

Regras:
1. Toda decisao arquitetural relevante vira ADR ANTES do codigo.
2. ADR aceito so muda por novo ADR com `Supersedes`.
3. Formato: Contexto / Decisao / Consequencias / Supersedes (usar `template.md`).

| ADR | Titulo | Status |
|---|---|---|
| 0001 | CIB Seven como backbone de governanca; LangGraph como runtime de agentes | Accepted |
| 0002 | Estado de agentes em 3 camadas (working/episodica/semantica) | Accepted |
| 0003 | A2A v1.0 para colaboracao; Kafka para fatos | Accepted |
| 0004 | Instancia por tenant + Agent Definitions federadas L0-L3 | Accepted |
| 0005 | HITL como garantia arquitetural | Accepted |
| 0006 | PHI em duas zonas; pseudonimizacao no gateway | Accepted |
| 0007 | Identidade de agente, auditoria e nao-repudio | Accepted |
| 0008 | Niveis de autonomia L0-L3 por acao | Accepted |
| 0009 | Portfolio de modelos com abstracao de provider + eval gates | Accepted |
| 0010 | Observabilidade de agentes | Accepted |
| 0011 | Repo greenfield; hospitalar como referencia; licoes estruturais | Accepted |
| 0012 | DMN como ferramenta deterministica unica | Accepted |
| 0013 | Integracao Tasy via CDC da Plataforma de Dados; simulador como contrato | Accepted |
| 0014 | Plataforma de observabilidade: AMP/AMG gerenciados vs Prometheus/Grafana in-cluster | Accepted |
| 0015 | Runtime de delegacao A2A: Agent Card registry, envelope e anti-loop estrutural | Accepted |
| 0016 | Allowlist de process_key + invariante de pseudonimizacao no ToolRegistry | Accepted |
| 0017 | Enforcement de rede do egress da Zona PHI (CIDRs IP-fixados, fail-closed; FQDN via egress proxy/Cilium e FUTURO, nao entregue) | Accepted |
| 0018 | Padrao estrutural no-denial: replicacao vinculante de 5 partes para todo SP-OP negativa-like | Accepted |
| 0019 | amh-data-platform como lake-of-record; surrogate mpi_id<->fhir_patient_id; snapshot de feature-store | Accepted |
| 0020 | Chain-of-custody como PROJECAO sobre a cadeia de auditoria ADR-0007 (nao fork) | Accepted |
| 0021 | Dependencias in-cluster (CIB Seven, HAPI-FHIR) via StatefulSet | Accepted |
| 0022 | MCP servers registram tools IN-PROCESS no boot; sem Deployments stdio/SSE out-of-process | Accepted |
| 0023 | Politica de merge — main protegido com required status checks + strict up-to-date | Accepted |
| 0024 | Idempotencia DURAVEL dos drivers inbound/resume reusa o padrao Postgres | Accepted |
| 0025 | PEP ↔ Policy Unification — autonomy-matrix YAML loading + single action vocabulary (T1.8) | Accepted |
| 0026 | Worker standardization — adapter (FunctionWorker) sobre subclassing; 16→17 modulos registram no WorkerRegistry com retry/metrics (T1.2; 17o = raw-handler `events.py`, ver amendment note no ADR) | Accepted |
| 0027 | Audit Transport — Postgres-first (amends ADR-0007's Kafka assumption) (T1.10) | Accepted |
| 0028 | Avaliacao de DMN em runtime — engine-side (CIB Seven REST), fail-closed (T1.4) | Accepted |
| 0029 | Poda de audit_chain via checkpoint assinado + re-anchor; BLOQUEADA ate ADR-0020-amend + este ADR ratificarem (T2.8) | Proposed |
| 0030 | Semantica de erro de worker vs boundary catches do BPMN — `WorkerBpmnError` modelado (opt-in por codigo gate-proven), incidente para todo o resto (T3.1) | Accepted |
| 0031 | LGPD DSR identity gate fail-closed em `identidade_verificada is True` + GAP-LGPD-6 raise + #55 R-B request_additional_proof (T2.8) | Proposed |
| 0032 | Corrige a alegacao de "runtime completo" do ADR-0015 (e do donor v1 PR #17, nao do v2); amends ADR-0015, nao supersede (T3.4) | Accepted |
| 0033 | Identidade de servico do agente — emissao/verificacao do certificado/service-account (metade cert de T-G); espaco de decisao + recomendacao nao-vinculante; amends ADR-0007, nao supersede | Proposed |
| 0034 | L2 review sampling intencionalmente fora do v2 (descope ratificado, #24); autonomia enforce-ada estruturalmente (ADR-0018), nao por PEP em runtime; amends ADR-0008 + ADR-0025, nao supersede | Accepted |
| 0035 | PHI pseudonymizer — keyed HMAC-SHA256 (fecha SHA-256 sem chave reversivel); prod fail-closed em `PHI_HMAC_KEY` ausente, dev determinista por-tenant nao-secreto; cutover limpo (sem store persistido); amends ADR-0006, nao supersede (t6-hmac) | Accepted |
| 0036 | Identidade conversa/thread/business-key com HMAC keyed (fecha reversibilidade residual do ADR-0035 no `hash_phone`/`conversation_id` que o T4b passou a persistir + no Cockpit); trava do thread-id exige marcador keyed `hk1_` (nao mais so "nao-numerico"); fold-in `LogScrubber` fail-closed; cutover limpo; estende ADR-0035 + ADR-0006, nao supersede (t9-phi-conversation-id) | Accepted |
| 0037 | Fronteira de compatibilidade AMH — contratos canonicos AMH-owned (work-items/consent/outcomes v1 + APIs subject-context/population-features + contract manifest; pin imutavel `config/integrations/amh/contracts.lock.json`); supersede PARCIAL do ADR-0013 (clausulas 1, 2 [wire dev-JSON], 3, 4, 5; principios consume-not-duplicate e TASY-write-DROP preservados e re-ancorados); amends ADR-0034 (re-introducao do chokepoint por chamada, condicionada a aceitacao + MZO-040); RATIFICADO pelo owner 2026-08-03 (DL-0040) — metade Maezo do XRG-1 FECHADA; XRG-1 completo aguarda AMH-000 (MZO-000) | Accepted |
| 0038 | Emenda a proibicao 6 do ADR-0037 — reconciliacao da FORMA das chaves CIB com a realidade implantada (business keys hifen-primeiro `{PREFIXO}-{tenant}-{ids}`; process keys `SP-OP-<DOMAIN>-<NNN>`) + correcao da auto-inconsistencia XRD-08 (`maezo-payer/*`) vs proibicao 6 (`maezo-payer-`); DUAS opcoes com recomendacao NAO VINCULANTE (1 = grandfathering da forma viva, semantica preservada; 2 = manter forma congelada + janela dual-read com alias legado); amends ADR-0037, nao supersede, e NAO emenda a proibicao 5 — a perna (c) de DL-0043 (PHI/ID cru em keys) permanece ABERTA. Redigido em DL-0044; **escolha da opcao PENDENTE DO DONO** | Proposed |
| 0039 | Assinatura do envelope de delegacao A2A — digest canonico sobre {tenant, task_id, origin, target, payload_hash, deadline, budget, delegation_chain} (reusa a canonicalizacao `sort_keys`+separadores compactos ja usada por Card signing/audit chain); key-id + scheme + replay_epoch amarrados no digest; verificacao fail-closed dentro do dispatcher (novo `RejectionReason`), gate espelhando `_require_signer_or_fail_closed`; idempotencia durável + outbox transacional viram MANDATORIOS fora de dev-local (legs 2-3); mTLS DEFERRED ate existir transporte remoto; nao interage com a fronteira ADR-0037. Onda 3/Train C leg 1/4; extends ADR-0003/0007/0015, nao supersede; complementar a ADR-0033 | Accepted |
| 0040 | Perspectiva de processo — a operadora (pagador) e o dono de SP-OP-CONTAS-001 e SP-OP-RECURSO-001, nao o prestador. Os dois processos sao redesenhados do zero na perspectiva do pagador (RECEBE lote/recurso, decide e EMITE demonstrativo/glosa/resposta); a aresta CONTAS→RECURSO e deletada (RECURSO passa a nascer do recurso que o prestador transmite); guards novos `ERR_CONTAS_GLOSA_NOT_HUMAN`/`ERR_RECURSO_INDEFERIMENTO_NOT_HUMAN` substituem, um-para-um, `ERR_GLOSA_ACCEPT_NOT_HUMAN`/`ERR_DESISTENCIA_NOT_HUMAN`; gate de CI de regressao de vocabulario (Tier A/B, sem novo alvo de Makefile/workflow); `KNOWN_PROCESS_KEYS` inalterado (as mesmas 15 chaves). Amends ADR-0018 (instancia do efeito adverso; padrao de 5 partes permanece intacto) e re-ancora ADR-0030 (censo de boundaries), nao supersede nenhum dos dois. Implementado em 3 PRs de owner-review subsequentes (PR-3 RECURSO, PR-4 CONTAS) | Proposed |
| 0041 | Reconciliacao documental de sete ADRs Accepted com a arvore (GAPs AF-02/03/04/10/15/17/18): 0024 (`PostgresDedupeStore` nunca existiu + sujeito da ADR removido), 0005 (mecanismo 1 CONSTRUIDO porem `modo: shadow`, matriz so alcancada onde ha visao de capacidades, 3 superficies `choked: false`), 0008 (`src/maezo/policies/autonomy/` nunca existiu; matriz em `spec/`), 0032 (ADR datada — o runtime A2A existe desde `a4a4e2e`/#156), 0015 (`config/topic_registry.yaml` nao existe e `validate-artifacts` nao ve topicos), 0006 (nao ha mapa de reidentificacao — HMAC keyed irreversivel), 0012 (pipeline `contract_extraction` nunca portado); **amends** as sete, **nao supersede** nenhuma, e **nao edita** os arquivos delas (byte-identicos a `71dd4da`, cercado por `tests/unit/docs/test_adr_amendments.py`). Redigido por AGENTE — **ratificacao humana PENDENTE** | Proposto — DRAFT/verify |
| 0042 | Piso de k-anonimato e allowlist FECHADA de features no egresso de agregados do lago (AND-02/AND-S2, ADR-P-003); implementa ADR-0019 §4 (`0019:74-79`), que exige piso e proibe hardcodar `k` baixo — o gate `andre/graph.py::_scrub_aggregate` testava `k_anonymity < 1` (k=1 passa) e nao inspecionava `metrics`. A perna agent-buildable (validacao chave-a-chave de `metrics` + `config["min_k_anonymity"]`, default 1 sem flip de comportamento) esta em `fleet/and02-metrics-egress-gate` (nao mergeado). **Decisao do DONO: o VALOR do piso (relatorio propoe 5) + o conteudo da allowlist; requer DPO/juridico/regulatorio por ADR-0019.** Dependencia externa `mcp-datalake` WB.4. Redigido por AGENTE — ratificacao humana PENDENTE | Proposed — DRAFT/verify |
| 0043 | Memoria episodica/semantica declarada em 11/11 `agent.yaml` e exercida em 0 grafos (CC-07/HEL-09/AF-11, ADR-P-004): `MemoryServer` recusa fail-closed. O que falta NAO e o substrato: `pgvector` e `agent_memory.embedding vector(1536)` existem desde `0001_schema_agents.py:28,72` e `0006` nao os remove — faltam as tabelas doadoras (`episodic_events`/`semantic_memory`, nomes divergentes do schema vivo) e, sobretudo, um ESCRITOR (zero writers, sem indice de similaridade, sem composition root). NAO confundir com a camada de TRABALHO, que existe e esta viva (`runtime/checkpoint.py::Checkpointer` compilado em `HelenaDispatcher.dispatch`). **Decisao do DONO sobre a metade EPISODICA: ATIVAR por flag de build (wire sobre `agent_memory`), ou APOSENTAR o `MemoryServer` — status quo nao e oferecido. A metade SEMANTICA (pgvector) ja foi decidida pelo dono (R-005/R-006, 2026-09-04): opcao B, remocao via `0009_drop_pgvector`, com emenda DRAFT ao ADR-0002 §3, implementada em `r5/pgvector-remocao`.** Medida provisoria = desdeclarar apenas os flags `memory:` (ja escrita em `fleet/w3-docs-sweep`, 11 yamls + cerca `test_agent_memory_declaration_fence.py`); retirar `mcp-memory.read_write` NAO faz parte dela — quebraria `check_effect_chokepoint_fence.py:1034` e a excecao divulgada de `effect_classes.py`. Reconcilia e sequencia ADR-0002, nao supersede. Redigido por AGENTE — ratificacao humana PENDENTE | Proposed — DRAFT/verify |
| 0044 | `task_kind` catalogado e OBRIGATORIO em toda chamada LLM + gate de conformance no CI (CC-12/BEA-01/AF-12, ADR-P-005). O runtime resolve (`runtime/inference/__init__.py::InferenceProvider._resolve_task_model`, `_validated_model_tiers`, `AgentDefinition.model_tiers`) mas so lucas passa o kwarg (`lucas/graph.py:755,806,832`); os outros 9 grafos + `_template` nao. **Efeito HOJE e TELEMETRIA, nao custo** — modelo unico, `resolution="modelo_unico"` e o unico valor produzido. O catalogo fechado `MODEL_TASK_KINDS` (`inference/__init__.py:282`) JA existe com `batch` e JA e fail-closed sobre o mapa `model:` do yaml (`_validated_model_tiers`); o que falta e enforcement sobre o ARGUMENTO `generate(task_kind=...)`, que hoje degrada para `TIER_UNDECLARED`. **Decisao do DONO: aprovar o gate em `scripts/ci/`+`Makefile` (dono-gated) e se um call site pode usar `batch`.** Reforca o cumprimento de ADR-0009 decisao 2 (`0009:10-12`), nao emenda. Redigido por AGENTE — ratificacao humana PENDENTE | Proposed — DRAFT/verify |
| 0045 | Fail-notify canonico da falha de `start_process` — desfecho `erro_inicio_processo` como requisito do contrato canonico (CC-01/RAF-02/LUC-05, ADR-P-007; familia P1 #1 do fleet audit). Hoje 9/10 grafos convertem `CibSevenError` em dict e seguem por aresta INCONDICIONAL a um terminal no-op: fail-open processual, invisivel a `MaezoAgentCrashLoop`, e em Rafael um falso `COMPLETED` gravado no cache de idempotencia do dispatcher (irretentavel por `task_id`). O desfecho e estado do AGENTE, nunca do engine (sem `bpmnErrorCode`, sem entrada na allowlist BPMN). Padrao implementado em `fleet/template-canonical-contract` (`TemplateGraph.notify_start_failure`, `DESFECHO_ERRO_INICIO_PROCESSO`, `_route_after_start`), em propagacao em `fleet/cc01-start-fail-notify` (ambos nao mergeados). **Decisao do DONO: elevar a requisito do contrato canonico + politica de retry/alerta + a correcao da fronteira A2A.** Redigido por AGENTE — ratificacao humana PENDENTE | Proposed — DRAFT/verify |
| 0046 | **MEMO ao dono, sem recomendacao de engenharia** — agendamento direto de Helena (HEL-11, ADR-P-001). A proposta da auditoria (L2 com DMN de elegibilidade) seria um REBAIXAMENTO: `scheduling` ja e L3 em `spec/policies/autonomy/L0-core.yaml:31`. A capacidade exige contrato novo SP-OP-AGENDAMENTO-001 + chave em `KNOWN_PROCESS_KEYS` (`tools/process_allowlist.py`, dono-gated com security-team, `.github/CODEOWNERS:65`) + ADR proprio. Tres perguntas ao dono; nenhuma linha deve ser escrita antes da resposta. **Decisao PENDENTE DO DONO** | Proposed — MEMO |
| 0047 | Emenda a ADR-0002 §3 (GAP DU-01-b): a camada SEMANTICA fica **SUSPENSA ate existir consumidor** — nao negada, nao superseded. A camada foi desenhada e ratificada em 2026-07-05 e NUNCA teve consumidor (`grep -rn 'ivfflat\|hnsw' src/maezo` -> 0 hits; zero writers de `agent_memory.embedding`; `mcp_memory.recall_semantic` recusa fail-closed), pagando extensao, parameter group do Aurora e imagem de compose. `0009_drop_pgvector` remove coluna e extensao; `,pgvector` sai de `shared_preload_libraries` (GAP DU-03); o compose passa a `postgres:16`. §1/§2/§4 e a negativa aceita de `0002:18` permanecem; **nao edita** o arquivo de ADR-0002 (convencao de `README.md:8` + ADR-0027/0032/0041). Decisao do dono R-005/R-006; redigido por AGENTE — **ratificacao humana PENDENTE** | Proposto — DRAFT/verify |
| 0048 | `driver_idempotency` re-ancorada como registro de dedup por `wamid` do webhook WhatsApp (decisoes do dono R-071 opcao C, R-072 e R-073 opcao 2): a tabela orfa da ADR-0024 vira o registro duravel de dedup do canal, com a migracao `0010_webhook_wamid_dedup.py` declarando o novo uso no proprio schema e adicionando a coluna `status` do protocolo claim/commit; a chave persistida e o pseudonimo COM CHAVE do `wamid` (ADR-0035), nunca o `wamid` cru. **Re-ancora** a ADR-0024 (Decisao 3) e marca como DATADA a §1(d) da ADR-0041 ('tabela orfa'), sem editar nenhuma das duas — a nota de re-anchor que R-073 pede dentro da 0024 e impossivel sem violar `README.md:8` e a cerca de bytes `tests/unit/docs/test_adr_amendments.py`. Redigido por AGENTE — **ratificacao humana PENDENTE** | Proposto — DRAFT/verify |
| 0049 | Portal humano pt-BR para colaboradores, beneficiários e prestadores; substitui a escolha histórica Tasklist-only segundo o plano autorizado do usuário (`PLAN-PRODUCT-PORTAL-SUPERSEDE-TASKLIST`, DL-0048). Gateway humano sempre enforcing, comando Java/CIB 2.1.0 assinado com transação/receipt e audit intent + outbox humano transacional. Amends ADR-0004/0006/0007/0017/0021/0027/0037 nas cláusulas delimitadas; ECS/Fargate sa-east-1 substitui o alvo EKS nesta implantação, com invariantes PHI/tenant preservados. Autoria de AGENTE; sem ratificar decisões clínicas/DPO/produção. | Accepted apenas decisão de produto/engenharia do plano; especificação DRAFT/verify e implementação pendentes |
| 0050 | **Um dominio de idempotencia por guia em SP-OP-AUTH-001** (decisao do dono #16, 2026-09-12, opcao (a)). O canal do portal montava `AUTHI-{guide_identity_ref}` a partir de uma referencia OPACA enquanto contrato/BPMN/canal de agente usam `AUTH-{tenant_id}-{numero_guia_tiss}` — duas chaves bem-formadas para a MESMA guia, logo DOIS dominios de idempotencia: os canais nao se enxergavam, a mesma guia podia ter duas instancias vivas com duas `UT_AnaliseMedicoAuditor`, e `correlate_message` por business key era inutil para instancias do portal. Passa a existir UM compositor (`tools/process_business_keys.py::auth_business_key`, cercado por AST sobre `src/`), componente vazio e' RECUSADO (as f-strings antigas colapsavam para `AUTH--`), a chave legada e' recusada por nome e NUNCA convertida (nao ha funcao de opaco para numero de guia; 0 instancias de producao a carregam — o caminho que a montava nunca teve chamador), e `SP-OP-AUTH-001` sobe de `NON_STRICT` para **`EXCLUSIVE`**. NAO `PERMANENT`, por duas razoes independentes: o reuso cross-time de numero de guia e' pergunta MEDICA/ANS em aberto, e o canal do portal nao atravessa o portao (`start_process_idempotent` desvia `HumanIntakeProvenance` antes de ler a postura), entao `PERMANENT` seria permanente por uma porta e ausente pela outra. Emenda a classificacao de B-3/GAP-D3-02; nao edita ADR aceita. Redigido por AGENTE — **postura-alvo a ratificar** (medico-auditor / ANS-regulatorio) | Proposed — decisao de dominio TOMADA (#16); postura-alvo DRAFT/verify |

Texto integral e racional estendido: `agent-platform-adrs.md` no projeto de arquitetura
(serie AP-001..AP-012 mapeia 1:1 para 0001..0012 desta serie).
