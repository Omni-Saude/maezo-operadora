# SAME independent diagnosis — ROOT native 8de77 actual35PASS/8FAIL

**REVISE native D7-B before acceptance.** ROOT's exact real Java17/CIB2.1/PostgreSQL run executed all43 declared WorkloadEngineIT invocations: **35 PASS, 8 FAIL, 0 ERROR, 0 SKIP**. The eight failures are four distinct diagnostic groups, not one shared failure cause. Assign distinct narrow product and test-observation repairs as specified below; preserve the original actual failure evidence and assertions.

ROOT ran exact `8de77e92f9782dff69365fd06d5eca6b4899d051` with wrapper `c69df804ac7a7ea80606a937486407d815f7cb9e236614baefa77b67364bccac` and canonical runner `e567c811b978c8c5c0d9135c92a9b951b73abf6bb1d2ce5ecc846c0776a72908`. The wrapper state spans 2026-09-09 09:08:44.507–09:09:57.978 UTC and reports Maven1, return1, maven_complete, cleanup_complete=true. Exact launcher argv/split streams, full Maven log/JUnit and full generated target are retained unchanged in `root-actual/`.

Independently rehashed all64 archived Maven regular outputs/649936bytes and all1505 execution-source hashes against the clean independent 8de77 worktree. Source is unchanged and the execution clone is absent. ROOT's final custody reports project and canonical lease absent. This reviewer performed no service/engine/Docker/PG/full-unit/global-ledger/GitHub operation or source repair. This is read-only diagnosis of ROOT's actual execution, not a second live run or final gatekeeper verdict.

## Exact failure groups

| Actual JUnit identity and parameter | What the run demonstrates | Bounded diagnosis |
|---|---|---|
| `afterNativeFlushWaitAuthorityIsRecheckedAtCommitting(String)[3]`, native-grant-revoked | Actual user-task INSERT was observed blocked, UPDATE_INSTANCE was removed, and the operation returned without the required refusal. Failure at line316. | Product grant freshness missing after last SQL / at COMMITTING. |
| `realSourceAuthorityCannotExpireAfterLastTargetSql(String)[3]`, native-history-revoked | Actual target external-task INSERT was observed blocked, source READ_HISTORY was removed, and the operation returned without the required refusal. Failure at line508. | Same missing grant-recheck lifecycle, for required source-history authority. |
| `priorHumanEvidenceRequiresActualTaskDecisionProvenance(String)[5]`, wrong-definition | The source task's historic definition ID was changed from approved CONTASv2 to a real CONTASv1 ID; typed operation returned instead of Refused403. Failure at line431. | Product exact historical source-definition consistency missing in the prior-human branch. This is a deliberately hostile persisted-provenance fixture, not a demonstrated public DB-mutation capability or real clinical/financial decision. |
| `authorityChangesWhileActualExternalRowIsBlockedRollback(String)[5]`, native-grant-revoked | After actual row wait and UPDATE_INSTANCE removal, the native engine threw AuthorizationException. The helper expected Refused503 and failed at line207/285. | Native denial occurred; this is a typed freshness-refusal contract mismatch, not a successful permission bypass. |
| `competingNativeOwnershipActionAndTypedCompleteAreSerialized(String)[1..4]`, complete/unlock/extend/timer | First competing native SQL wait at line459 passed. All four failed at the **second**, typed SELECT wait at line462 after about10s; outcome assertions were not reached. | Test observation/precondition failure. An indirect blocking chain is plausible, but the exact live lock graph was not recorded and cannot be asserted as the proven cause. |

`case-map.json` maps all43 JUnit IDs to their exact source ValueSource parameter strings and source frames, avoiding guesses from method names or numeric indices. The first three successful-return failures stop before their subsequent snapshot/no-effect assertions. Thus the evidence proves unexpected operation return after those precise conditions, but this packet does not invent post-failure database row counts or claim the skipped snapshot assertions passed. The native exception case also stops before its snapshot/recovery checks. The four race cases do not prove any final race outcome.

## Source explanation and preserved actual controls

`WorkloadCommand.execute` checks capability grants once before attestation and blocking work. `current()` checks native identity/configuration, mounted policy and lock deadlines; the COMMITTING listener calls that same function. It does not recheck the selected capability's native grants. That source path explains both successful-return revocation failures and why the early-row case reaches a later native AuthorizationException. `WorkloadServlet` has a safe native-exception403 fallback, so no PHI leak is demonstrated; that fallback does not satisfy the original native Refused503 freshness assertion.

The locked-source prior-human branch selects a historic task by process, tenant and task-definition key, validates decision receipt/assignee and historical variable update, but omits the exact source definition ID/version consistency check. The direct completed_human source branch already performs the definition check. The successful wrong-definition negative is therefore a concrete failure of the already-required source binding, not a reason to relax the test or invent a human-positive fixture.

The race helper only tests whether the original blocker PID appears directly in `pg_blocking_pids` for any matching SQL substring. It neither identifies the submitted future's backend nor follows an intervening competing session. PostgreSQL16 documents blocking by preceding incompatible waiters as well as lock holders. That makes the queued competing-session explanation plausible; actual PID linkage must be captured before classifying or repairing this run's observation. [PostgreSQL16 system information functions](https://www.postgresql.org/docs/16/functions-info.html#FUNCTIONS-INFO-SESSION-TABLE).

The35 actual passes are material and must be preserved: original3 native methods; all5 readiness/execution grant-removal cases; both native command-context fences/background controls; four blocked-row ownership/policy fault controls; both last-SQL policy-loss controls; both D5 enlisted-receipt policy-loss rollback controls; optimistic committed fetch; six real resource/identity mismatch controls; seven other prior-human negative cases; both source-expiry/policy-loss last-target-SQL controls; and missing typed native authentication. They do not close the eight failing requirements or replace the separate D5 preservation37, original image5 or D6 lanes.

## Distinct repair mandates and ROOT next steps

`PRODUCT-REPAIR-MANDATE.md` is D7B-NATIVE-GUARD-01: narrow product work in WorkloadCommand.java and WorkloadPlugin.java only, preserving all existing tests. Revalidate current native grants at the required post-block/last-precommit points without stale cached authority, grant broadening, fence bypass or D5 transaction/listener weakening; normalize the specific typed refusal; bind prior-human history to exact approved source definition. Keep source READ_HISTORY required in the approved automatic empty-evidence branch. Existing actual failures are the regressions, with all later no-effect/readiness/same-pending recovery assertions still mandatory.

`RACE-OBSERVATION-MANDATE.md` is D7B-RACE-ORACLE-01: test-only instrumentation/observation in WorkloadEngineIT.java. Capture exact owned blocker/competitor/victim PID relationships and bounded wait evidence, then recognize a proven direct or identified indirect chain while rejecting unrelated/cyclic/missing waits and preserving every final strict native result/state assertion. Do not treat timeouts, increased sleeps, skips or any matching global SQL as a race proof. The product and test repairs should be authored distinctly from this verifier; no implementation is included here.

ROOT should qualify each exact frozen successor through SAME, combine them with the already-qualified static TempDir and HTTP a07269e0 deltas with full preservation/source custody, then rerun the unchanged qualified PG wrapper's workload lane on the combined clean SHA with a new output leaf after ROOT's current serialized gate finishes. All43 actual bodies must pass without skipped assertions. If the wait graph reveals another cause or the guarded recheck encounters native phase/cache behavior, retain the first exact failure for narrow diagnosis instead of broadening accepted outcomes. Then run the qualified preservation lane and required original fixtures. No new service wrapper or Maven selector broadening is required for the full43 rerun.

The HTTP ACCEPT-02 source repair is independently qualified in `completion-portal-d7-http-acceptance-independent-delta-a07269e0`, but its actual secured-image execution still requires a separately reviewed image wrapper authorizing every test-time Docker command. Neither that HTTP source verdict nor these35 native passes qualifies production cutover, genuine human PAGTO/consent activation, C callers, D bootstrap, E legacy fixture disposition, missing reviewed schemas or either reserved final goal gatekeeper verdict.
