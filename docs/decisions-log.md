# Decision Log — Maezo Healthcare Plan

Decisões não-triviais do orquestrador/swarm que não justificam ADR próprio.
Decisões arquiteturais relevantes continuam virando ADR (`docs/adr/`).

Formato: `| data | id | decisão | racional | quem |` — uma linha por decisão, mais recente no topo.

| Data | ID | Decisão | Racional | Quem |
|---|---|---|---|---|
| 2026-09-04 | DL-0047 | **`ERR_CONTAS_LOTE_INVALIDO` e `ERR_CONTAS_GLOSA_NOT_HUMAN` permanecem DECLARADOS-E-NAO-CAPTURADOS POR DESENHO em `SP-OP-CONTAS-001` — o dono CONFIRMOU a leitura do implementador (`OWNER-DECISIONS-REGISTER` R-097, gap `CONTAS-DEAD-ERROR-CATALOG`/M-71 pergunta 1: *"SIM — confirmar a leitura; nenhuma acao de codigo hoje"*). NENHUMA acao de codigo: o catalogo `bpmn:error` do BPMN nao e editado, nenhum `errorEventDefinition`/boundary e modelado e `scripts/ci/check_bpmn_error_allowlist.py` nao muda. ESCOPO DA RATIFICACAO — esta linha ratifica a LEITURA (a de que o estado declarado-e-nao-capturado ja e o estado ratificado do artefato), **nao a ADR-0040**, que segue `Proposed` e cuja ratificacao continua sendo ato humano sob CODEOWNERS de `/docs/adr/`. **R-098 (M-71 pergunta 2), registrado junto:** se o gate `check_bpmn_error_allowlist` algum dia endurecer para cobrir declaracoes-raiz sem boundary, isso sera uma **decisao de programa unica** — ADR de convencao ratificada ANTES de qualquer correcao — nunca 13 correcoes ad-hoc; nada muda hoje e nenhuma ADR nova e criada por esta linha. | A decisao de fundo ja e **Accepted** em `docs/adr/0030-worker-error-semantics-bpmn-boundary.md:379-382` (guard sem boundary modelado -> incidente) e `:358-365` explica que ativar qualquer guard pre-T-E converteria um incidente humano-visivel numa saida silenciosa — regressao de visibilidade sob a invariante HITL, nao melhoria. Remover as declaracoes seria INERTE: `check_bpmn_error_allowlist._parse_bpmn_file` so admite um `bpmn:error` em `spec_codes` depois que um `errorEventDefinition` o referencia, entao nem a clausula (b) nem a (c) do gate alcancam uma declaracao-raiz — apagar so removeria a ancora documental que a propria ADR cita pelo nome. O alcance do endurecimento futuro e de PROGRAMA porque sao 20 de 43 declaracoes-raiz em 13 de 16 arquivos BPMN, incluindo `ERR_CANCELLATION_NOT_HUMAN`/`ERR_DENIAL_NOT_HUMAN` — os precedentes que a ADR-0030 cita. Codigo/docs desta disposicao ja haviam entrado pelo PR #302; o que faltava era o reconhecimento registrado. | dono (arquitetura) via `OWNER-DECISIONS-REGISTER` R-097/R-098; registrado pelo agente |
| 2026-09-03 | DL-0046 | **POLÍTICA DE DEDUP DE START passa a ter TRÊS estados; `SP-OP-CANCEL-001` recebe o novo estado `EXCLUSIVE` — e a recomendação literal da auditoria ("estender a política estrita a CANCEL") é APLICADA NO ESPÍRITO E RECUSADA NA LETRA, com o motivo registrado.** Contexto: o `_START_DEDUP_POLICY` (`src/maezo/tools/mcp_cibseven/transport.py`) nasceu no fix do BLOCKER B-3 (PR #212, `PLANS.md` §0.7 — onda 1 do sprint de 2026-08-09) e **nunca teve linha própria neste log**: o critério vivia apenas no bloco de comentário do próprio mapa. Esta linha registra o critério E a sua extensão. **O QUE FOI DECIDIDO.** (1) O booleano vira `StartDedupPosture` com três valores: `NON_STRICT` (o claim durável deduplica só a LINHA DE AUDITORIA; o efeito é gated apenas pelo `find_active_instance`), `EXCLUSIVE` (o claim é token de EXCLUSÃO MÚTUA entre starts CONCORRENTES, resolvido contra evidência do engine — instância viva é devolvida, instância PROVADAMENTE ENCERRADA libera o próximo caso legítimo da chave, e "nenhuma instância em lugar nenhum" continua INDECIDÍVEL-E-ALTO `StartClaimWithoutInstanceError`) e `PERMANENT` (tudo do `EXCLUSIVE` mais: instância encerrada RECUSA o start — `ALREADY_COMPLETED`). (2) `SP-OP-CANCEL-001` passa a `EXCLUSIVE`; `SP-OP-PAGTO-001` permanece `PERMANENT`; as outras 13 chaves permanecem `NON_STRICT` — entre elas `SP-OP-LGPD-DSR-001`, `SP-OP-REEMBOLSO-001` e `SP-OP-ADEQUACAO-001`, que estavam AUSENTES do mapa e caíam no default silencioso; classificá-las é mudança de comportamento ZERO (é exatamente o que o default já lhes dava) e torna o mapa COMPLETO contra `process_allowlist.KNOWN_PROCESS_KEYS`, agora com asserção de completude em teste (`test_every_known_process_key_is_classified_in_the_policy_map`), de modo que uma chave futura QUEBRA O BUILD em vez de defaultar em silêncio. Deliberadamente NAO se tornou o default fail-closed: isso deixaria as três chaves ININICIÁVEIS hoje, e recusar o start de um DSR de LGPD é por si só quebra de conformidade — o mesmo anti-padrão de efeito adverso fabricado por portão que este DL recusa em CANCEL. Chave não classificada continua defaultando para `NON_STRICT` com warning, agora como rede de segurança e não como política. (3) Nenhum fallback silencioso: um root de composição que não forneça `HistoryQueryingTransport` + `DedupReportingAuditSink` faz o start de família com portão RECUSAR (`StartDedupGateUnavailableError`) antes de qualquer escrita durável. | **Por que o booleano era a causa-raiz.** Ele soldava duas garantias separáveis: (a) exclusão mútua entre starts concorrentes — fecha a janela TOCTOU que o `find_active_instance` (um GET puro) estruturalmente não fecha; (b) portão PERMANENTE no tempo. CANCEL-001 precisa de (a) e não pode ter (b) — e é exatamente por isso que o autor do B-3 o deixou no balde "AMBÍGUO / decisão humana" em vez de decidir: com só `True`/`False` não havia classificação honesta a dar. **Por que (a).** Dois dos três start sites de CANCEL-001 são RE-ENTREGÁVEIS por construção (expiração de lock de external task re-despacha `inadimplencia.handoff_rescisao`; re-entrega Kafka re-dispara a regra FRAUDE→CANCEL da ponte). Uma SEGUNDA instância CONCORRENTE é o risco L0 de dupla terminação que `docs/processes/harmonization-inadimplencia-cancel.md` §1 existe para impedir: duas `UT_AnaliseRescisao` paralelas para um contrato. **Precisão devida (apontada pelo gatekeeper):** a §1 trata primariamente de dupla terminação CROSS-PROCESSO; a idempotência de start é a sua camada 2, e a camada 3 contempla explicitamente duas instâncias e as barra no efeito terminal ("mesmo que duas instâncias cheguem a um worker adverso, a segunda é recusada"). A citação continua válida, mas a severidade L0 do start duplicado é MENOR do que a redação anterior sugeria — e a própria camada 3 tem ressalva: `check-bpmn-error-allowlist` reporta `ERR_CONTRACT_SUSPENSION_NOT_HUMAN` como "dead model — declared boundary, no raiser", ou seja pode estar parcialmente NÃO CONSTRUÍDA. Fora do escopo desta PR; registrado como nota ao orquestrador. **Por que NÃO (b) — o achado que recusa a letra da recomendação.** A chave não é one-shot, e a prova está nos terminais do próprio processo: TRÊS end events de CANCEL-001 deixam o contrato VIVO (`End_ContratoMantido` `:467`, `End_PedidoCancelamentoNegado` `:443`, `End_ManterNaoConfirmado` `:419` em `spec/processes/bpmn/SP-OP-CANCEL-001_Cancelamento_Contrato.bpmn`) e UMA chave serve os quatro `tipo_solicitacao` (`pedido_beneficiario`/`inadimplencia`/`for_cause_operadora`/`fraude_referida`, contrato § Variáveis de entrada). Logo um titular cujo pedido de cancelamento foi NEGADO hoje, ou um contrato mantido por decisão humana MANTER, PLAUSIVELMENTE produz um SEGUNDO caso sob a MESMA chave depois. ⚠️ **`DRAFT/verify` — ESTA PREMISSA NÃO É FATO DE ENGENHARIA E NÃO ESTÁ RATIFICADA AQUI.** A BPMN e o contrato provam o MECANISMO (três terminais deixam o contrato vivo; uma chave serve quatro gatilhos); NAO provam que um caso recorrente sob o mesmo `numero_contrato` seja LEGALMENTE esperado — isso é pergunta de **jurídico/contratos**. DECISORES: jurídico/contratos (recorrência do caso), **ANS-regulatório** (RN 593 / RN 412). Nada em `docs/compliance/` confirma qualquer das duas hoje. **E a incerteza é a RAZÃO da escolha, não um furo nela:** `EXCLUSIVE` é a postura CONSERVADORA SOB essa dúvida — não nega um segundo caso (como `PERMANENT` faria) nem deixa starts concorrentes sem separação (como `NON_STRICT` deixa); é a única das três que não precisa da resposta do SME para ser segura. Se os SMEs decidirem que a chave é one-shot, mover para `PERMANENT` é uma linha, sob revisão. Sob `PERMANENT` esse segundo caso seria recusado com `ALREADY_COMPLETED` — **efeito adverso contra beneficiário produzido por um portão de idempotência, sem humano nenhum no circuito**, pior que o defeito que se buscava fechar. É o mesmo argumento de recorrência que mantém `SP-OP-INADIMPLENCIA-001` fora do portão permanente (inadimplência RN 593 recorre — ⚠️ `DRAFT/verify`, mesma âncora que a própria BPMN marca como não verificada em `SP-OP-CANCEL-001_Cancelamento_Contrato.bpmn:25-28`, decisor ANS-regulatório), e é o handoff da própria INADIMPLENCIA que cunha esta chave CANCEL. **Por que o efeito irreversível não muda isso.** Rescisão É irreversível, mas NÃO nasce do start: `contract_termination` é L0 hard e todo terminal adverso só é alcançável pela User Task humana `UT_AnaliseRescisao` (contrato § Terminais humano-gated). Um start duplicado compra uma REVISÃO humana duplicada, não uma rescisão duplicada — a assimetria exata com `SP-OP-PAGTO-001`, onde o efeito DECORRE do start. **Resíduo divulgado — CORRIGIDO, era subdeclarado.** A redação anterior dizia que, encerrada a geração reivindicada, a exclusividade da PRÓXIMA voltava a depender do `find_active_instance`. É pior do que isso, e a diferença importa: o dedup key é `(tenant, process_key, business_key)` e NÃO tem componente de geração, então o claim durável é cunhado UMA ÚNICA VEZ na vida da business key e nunca é re-cunhado. `EXCLUSIVE` portanto oferece exclusão mútua para a GERAÇÃO 1 APENAS: da geração 2 em diante, para TODA geração seguinte, PARA SEMPRE, dois starts concorrentes veem ambos `deduped=True`, ambos resolvem para "geração encerrada" e ambos caem no `find_active_instance` sujeito a TOCTOU — isto é, exatamente o comportamento `NON_STRICT` do `main`, nem melhor nem pior. Fechar isso exige token durável ESCOPADO POR GERAÇÃO — o mesmo outbox XRD-10/MZO-060 que este módulo não constrói. O mecanismo está fixado em teste (`test_the_durable_claim_is_minted_once_per_key_never_once_per_generation`). Por que ainda vale entregar: a rajada de re-entrega dominante (expiração de lock / retry de broker, segundos após o handoff) cai dentro da geração 1, que é a janela que esta postura de fato fecha, e nenhum racer de geração 2+ fica pior do que estava. **Co-requisito operacional.** A varredura por idade de `audit_emit_dedup` NÃO precisa excluir chaves `EXCLUSIVE` (o claim só vale enquanto a geração está em voo); a exclusão continua exigida para `PERMANENT`. **COMO um DBA implementa essa exclusão:** a tabela NÃO tem coluna de postura (migration 0005 = `tenant, dedup_key, record_hash, created_at`), então o único discriminador disponível em SQL é o segmento de process key dentro do próprio `dedup_key`, cujo formato é fixado por `start_dedup_key` como `{tenant}:start:{process_key}:{business_key}` — i.e. um predicado `dedup_key LIKE '%:start:SP-OP-PAGTO-001:%'` (uma cláusula por chave `PERMANENT`). Sem essa frase a relaxação era ininstrumentável. Registrado também que a relaxação vive no bloco de comentário de `transport.py` e em lugar nenhum mais: a mudança em `gateway/audit_postgres.py` foi SO de docstring, nenhum código de retenção foi alargado (não existe nenhum neste repo). Retenção de história do engine >= retenção do claim mantém o portão silencioso em ambas. **Recuperação de crash na janela claim→start — `EXCLUSIVE` inclusa, e o claim NÃO volta a ser reclamável.** Um crash entre o claim durável e o POST ao engine deixa o estado "claim existe, engine não tem instância nenhuma". Numa chave `NON_STRICT` a re-entrega simplesmente reinicia e converge para UM start. Em `EXCLUSIVE` e `PERMANENT` o mesmo estado é INDECIDÍVEL: é produzido igualmente por "o POST nunca teve efeito" (reiniciar seria o certo) e por "um concorrente ganhou o claim e o POST dele ainda está em voo" (reiniciar cria a SEGUNDA `UT_AnaliseRescisao` concorrente, ou o segundo pagamento). Fica registrado explicitamente, porque a redação anterior deste DL não dizia: **a ausência de instância NÃO libera o claim de uma chave `EXCLUSIVE`** — ausência de evidência nunca vira permissão, e é justamente a assimetria com o caso 2 (instância PROVADAMENTE encerrada é evidência POSITIVA de que a geração reivindicada acabou) que sustenta a postura. As duas posturas gated se comportam de forma IDÊNTICA aqui; a única divergência entre elas continua sendo a instância encerrada. O desfecho é o travamento ALTO `StartClaimWithoutInstanceError`, com ZERO starts, o claim intacto e o `dedup_key` nomeado. **Custo operacional declarado:** uma chave travada por crash exige intervenção humana — não há auto-recuperação, por projeto (fechar isso de verdade é o mesmo outbox XRD-10/MZO-060). **Runbook:** `docs/runbooks/engine-processes.md` §5, "Chave travada: claim duravel sem instancia" — confirmar no engine (ativa E histórica) → iniciar manualmente com a MESMA business key OU apagar aquela ÚNICA linha de `audit_emit_dedup`; apagar uma linha ali NÃO é cirurgia na cadeia (é tabela IRMÃ de `audit_chain`, migration 0005, sem elo de hash, invisível ao `verify_chain`), e a proibição de `docs/runbooks/audit-recovery.md` §3 segue ABSOLUTA sobre `audit_chain`; o efeito colateral correto do rearme é a cadeia GANHAR um elo (append-only). **Prova executável:** `tests/integration/chaos/test_crash_between_seams.py` — o B1b, que era UM teste, passa a ser três por postura mais um ensaio do runbook (`test_b1b_operator_rearm_after_the_gated_wedge_starts_exactly_once`). O teste antigo codificava a política ANTIGA: com `SP-OP-CANCEL-001` como sujeito ele exigia "converge para um start", que é exatamente o que `EXCLUSIVE` recusa — ele ficou VERMELHO na lane `chaos` da PR e é o único achado desta lane. O sujeito `NON_STRICT` passou a `SP-OP-NIP-001` (expectativa original intacta); nenhuma expectativa foi afrouxada para "qualquer um dos dois desfechos", o que passaria sob as DUAS políticas e não provaria nenhuma. **Reparo pós-gatekeeper (MAJOR-1) — parte da decisão, não detalhe de implementação.** A postura `EXCLUSIVE` cria MÚLTIPLAS linhas de história por business key POR PROJETO (encerrada uma geração, a próxima começa sob a mesma chave), e `find_any_instance` devolvia a PRIMEIRA linha que casasse: numa história `[gen-1 COMPLETED, gen-2 ACTIVE]` ela respondia COMPLETED, o portão lia "geração reivindicada encerrada", caía fora e INICIAVA UMA SEGUNDA INSTÂNCIA CONCORRENTE — precisamente a dupla `UT_AnaliseRescisao` que a postura existe para impedir, produzida PELA postura. Reproduzido pelo gatekeeper com engine mockado. Corrigido na raiz: predicado único `historic_instance_is_live` compartilhado pela LEITURA (`find_any_instance`) e pelo VEREDITO (`_resolve_strict_dedup_hit`), varredura de TODAS as linhas com preferência pela viva, e `FakeCibSevenTransport` deixa de ser uma célula por chave e passa a guardar LISTA (o double antigo NÃO CONSEGUIA representar história multi-geração, logo nenhum teste escrito contra ele poderia pegar essa classe de defeito). Sem esse reparo a decisão `EXCLUSIVE` não se sustentaria. **Fora de escopo, registrado como follow-up:** `SP-OP-RECURSO-001`, `SP-OP-AUTH-001`, `SP-OP-CRED-001`, `SP-OP-FRAUDE-001` e `SP-OP-CONTAS-001` também satisfazem o passo 1 do critério, mas cada um depende de uma pergunta de domínio (estabilidade de `glosa_id` externo; reuso de número de guia TISS — semântica médica/ANS; ciclo de recredenciamento; novo caso de fraude contra o mesmo prestador; correção de lote) que uma mudança de engenharia não pode responder sozinha. Nenhum foi alterado nesta PR. | idempotency-implementer (R1) + gatekeeper L0 independente (R1; veredito **REVISE**, 2 MAJOR + 7 MINOR, correções aplicadas por repair-engineer R1), GAP-D3-02 — **decisão de ENGENHARIA tomada sob o critério que JÁ EXISTIA no bloco de política (este DL registra o critério, não o inventa). A DIVERGÊNCIA em relação à LETRA da recomendação da auditoria — que pedia estender a política estrita/permanente a CANCEL — está declarada acima com a contra-evidência; o gatekeeper independente ACEITOU a divergência e confirmou que `PERMANENT` produziria efeito adverso contra beneficiário sem humano no circuito. A ratificação é do orquestrador, nunca do autor (ground rule 2), e a linha permanece PENDENTE DE CIÊNCIA DO DONO. Nenhuma premissa de SME marcada `DRAFT/verify` acima é dada por resolvida por esta linha.** |
| 2026-08-09 | DL-0045 | **MZO-040 CONSTRUÍDO "DARK" E LIGADO EM SOMBRA no chokepoint por-chamada, com o portão humano INTACTO — e o override de ambiente do manifesto proibido de ligar enforcement sozinho.** DUAS decisões, registradas juntas porque a segunda existe por causa da primeira. **(1) Construir antes de aprovar, inerte.** A XRD-09 da ADR-0037 só "produz efeito com a aceitação deste ADR + evidência MZO-040 entregue", e o MZO-040 está bloqueado nas aprovações Médica/ANS/Security (`PLANS.md` §0.6; o DL-0042 declara em termos que **não** as alcança). Isso é um laço: sem evidência não há aprovação, sem construir não há evidência. Decidido desfazê-lo na única direção segura — construir o gateway (`src/maezo/gateway/action_execution.py`), ligá-lo em SOMBRA em `WorkerHarness._handle` (avaliado ANTES do handler, porque o handler É onde o efeito acontece), e pôr o registro de aprovações como DADO em `spec/policies/autonomy/action-approvals.yaml` com **as 30 caixas vazias** (`aprovado: false`, campos `PENDENTE`). NENHUM bloco foi preenchido por agente; `status: DRAFT` + `modo: shadow`; toda classe NEGA em termos de enforcement e nada é bloqueado. Ratificar continua sendo ato humano de DADO (preencher blocos + completar `mapeamento_topicos` + `status: RATIFICADO` + `modo: enforcing`), sem uma linha de Python. O portão do `PLANS.md` §0.6 **permanece AINDA BLOQUEADO**, agora qualificado: bloqueado como decisão, com o gateway sombra existindo e negando até a aprovação. **(2) O override de caminho do manifesto não pode ligar enforcement.** `MAEZO_ACTION_APPROVALS_PATH` (espelhado do `auth_criteria`) troca o manifesto governado por qualquer arquivo — provado, num reparo pós-gatekeeper, capaz de ligar enforcement ao vivo com ZERO edições no arquivo listado no CODEOWNERS, o que tornaria a listagem decorativa. Decidido: um manifesto vindo do override lendo `modo: enforcing` resolve para o modo `shadow_override` e **nunca aplica**, a menos que o operador também exporte `MAEZO_ACTION_APPROVALS_ALLOW_OVERRIDE_ENFORCEMENT=1` (literal exato) — o caminho legítimo de rollout encenado; todo load vindo do override emite linha `error` com o caminho resolvido e se o enforcement foi permitido; e um ALLOW nessa condição carrega o token distinto `OVERRIDE_NAO_ENFORCAVEL`. Assim "trocar o manifesto" e "ligar o enforcement" viram dois atos deliberados e auditáveis em separado. Resíduo irredutível divulgado (§6 do pacote): quem controla o ambiente do processo pode setar as duas — é fronteira de deployment, remédio de plataforma, decisão do dono/Security. **REGISTRADO NO MESMO ATO, sem correção:** o CODEOWNERS deste manifesto é **listagem, não portão** — o `main` não tem proteção server-side (protection 404 + rulesets vazios, re-verificado; achado do dono já na row `mzo-000` do ledger), então o que de fato quebra o CI se o arquivo for apagado/alterado é o FENCE TEST (`tests/unit/sec/test_action_execution_fence.py`, rodado por `.github/workflows/ci.yml:61`), não o CODEOWNERS. As frases "CODEOWNERS-gated" do wave foram corrigidas para "CODEOWNERS-listed" em vez de mantidas. | Precedentes internos seguidos deliberadamente: `legal_bases_matrix.load_retention_matrix` (recusa o próprio template `unratified: true` em vez de "ter sucesso" sobre placeholder) e `auth_criteria.load_criteria_sources` (engole falha de load em "nada ratificado" em vez de levantar, porque um raise em caminho de worker vira incidente e incidente TRAVA pedido assistencial). A ADR-0034 descopou o chokepoint PEP por-chamada com UMA precondição de revisita — "SE e QUANDO o v2 introduzir um chokepoint de dispatch PEP por-tool-call em runtime" — e a XRD-09 é o veículo que essa precondição exige; construir inerte é o que permite que os três aprovadores ratifiquem contra o comportamento real em vez de contra uma descrição de projeto. Regra de não-fabricação: um agente **não** preenche bloco de aprovação (aprovação forjada é evento de conformidade, não bug) e **não** restaura proteção de branch nem emenda o portão por conta própria. A decisão (2) segue a mesma disciplina do fail-closed pin do repo (só o literal exato conta) e a lição do GK-criteria finding 1: não repetir alegação de "CODEOWNERS-gated" sem verificar o que de fato bloqueia. | orquestrador (Fable 5) + autor W5 + gatekeeper independente + repair-builder separado, 2026-08-09 — **as aprovações Médica, ANS e Security seguem ABERTAS; nada aqui as concede nem as antecipa** |
| 2026-08-09 | DL-0044 | **ADR-0038 REDIGIDO como Proposed (emenda à proibição 6 do ADR-0037 — FORMA das chaves CIB) — a DECISÃO DE RECONCILIAÇÃO DE DL-0043 CONTINUA PENDENTE DO DONO; este DL registra APENAS o ato de redigir, nunca o ato de decidir.** Esta linha NÃO ratifica, NÃO escolhe opção e NÃO altera o Status do ADR-0037 (que permanece `Accepted`, DL-0040) — o ADR-0038 nasce `Proposed` com o marcador detectável por máquina `RATIFICATION: PENDING — <owner act required>` e sem nenhum nome, data ou aprovação pré-preenchidos. O que o ADR-0038 faz: (1) converte as pernas **(a)** e **(b)** de DL-0043 (divergência de FORMA) em EXATAMENTE DUAS opções mutuamente exclusivas, de modo que o ato do dono seja escolher uma palavra — **Opção 1 (recomendada, não vinculante)**: emendar a proibição 6 para abençoar as formas implantadas (business key `{PREFIXO}-{tenant}-{ids}` hífen-primeiro; process key `SP-OP-<DOMAIN>-<NNN>`) como canônicas Maezo, cláusula de grandfathering que preserva INTEGRALMENTE os requisitos SEMÂNTICOS (opacidade, escopo de tenant obrigatório, ausência de PHI/ID cru de fonte) — raio de explosão de código ZERO; **Opção 2**: manter a forma congelada e agendar janela de dual-read com alias legado explícito, sem rewrite destrutivo (mecanismo da cláusula de rollback do MZO-020, citado via DL-0043); (2) registra e corrige, sob QUALQUER das duas opções — porém **por resoluções OPOSTAS (Opção 1 → `SP-OP-*`; Opção 2 → `maezo-payer-`)**, isto é, as duas opções eliminam a contradição, mas convergem para grafias contrárias, e a escolha do dono determina QUAL delas passa a valer —, a **auto-inconsistência do próprio ADR-0037** — `maezo-payer/*` (namespace com barra) em XRD-08 `0037:148` vs `maezo-payer-` (prefixo com hífen) na proibição 6 `0037:213`, duas grafias que não podem ser satisfeitas simultaneamente. **PERNA (c) EXPLICITAMENTE NÃO RESOLVIDA:** PHI/ID cru de fonte em business keys (proibição 5, `0037:210-211`) permanece ABERTA, rastreada em DL-0043, exigindo avaliação de privacidade própria (DPO/Legal/Security) — nenhuma das duas opções a fecha ou atenua, e aceitar o ADR-0038 não pode ser lido como aprovação dela. **NADA de código foi tocado:** diff docs-only estrito — nenhum `src/`, `spec/`, `tests/`, `deploy/`, nenhum worker, nenhuma BPMN, nenhuma allowlist, e o arquivo `docs/adr/0037-*.md` NÃO foi editado (emenda por ADR NOVO, per `docs/adr/README.md:8`). Verificação própria desta sessão, com `file:line` em todas as afirmações do ADR (compositores de key em 7 workers + os 7 do notification bridge + ~11 grafos/delegações de agente; `process_allowlist.py:60,21-42,131-135`; 20 `process id` `SP-OP-*` em `spec/processes/bpmn/*.bpmn` e ZERO `maezo-payer`; `identity.py:140,145,466-496` sem chamador de produção). **Correção de precisão a DL-0043 registrada no ADR:** `auth.py:1261` (`AUTH-{tenant}-{guia}-{uuid}`) NÃO é business key — é `numero_autorizacao` TISS (`auth.py:1267`); o compositor AUTH real é `agents/rafael/graph.py:169`. A forma e a conclusão de DL-0043 não mudam. **LIMITES DE VERIFICAÇÃO DECLARADOS:** (i) a cláusula de rollback do MZO-020 não é verificável neste repo — vive em `docs/prompts/`, gitignored (`.gitignore:43`) e ausente da worktree; citada de segunda mão via DL-0043, sem `file:line` primário inventado; (ii) NÃO foi verificado se o repo `amh-data-platform` depende literalmente da forma `{a}:{b}:{c}` ou do prefixo `maezo-payer-` — se depender, a Opção 1 exige emenda companheira do lado AMH; registrado como pergunta em aberto ao dono, não como fato. | Precedente **ADR-0033/DL-0035** (ADR produzido para ACELERAR a decisão humana, com opções + recomendação explicitamente não vinculante, nunca para substituí-la) + **ADR-0032/DL-0032** (um ADR corrige/emenda outro ADR aceito por ADR NOVO, achado documentado sem ação unilateral). A regra de não-fabricação de DL-0043 é honrada literalmente: um agente não reescreve chaves de processo implantadas nem emenda um ADR aceito por conta própria — por isso a emenda é PROPOSTA, não aplicada, e o `Status` do ADR-0037 não é tocado. O anti-padrão que este trabalho evita por construção é o de um redator alterar um literal de wire "por consistência" enquanto o ADR que o governa ainda está em disputa. | orquestrador-routed R1 author, 2026-08-09 — **decisão de reconciliação (Opção 1 vs Opção 2) PENDENTE DO DONO**; revisão independente a seguir |
| 2026-08-05 | DL-0043 | **ACHADO DE CONFORMIDADE REGISTRADO, NÃO CORRIGIDO — o esquema de chaves CIB em PRODUÇÃO diverge da proibição 6 do ADR-0037 na FORMA; a reconciliação é decisão do dono.** Levantado pelo autor do MZO-020 ao implementar a composição de business key exigida pela proibição 6, e verificado de forma independente. FATOS: (a) as business keys vivas são **unidas por hífen e com o prefixo à frente** — `ANSSUB-{tenant}-{report_type}-{competencia}` (`src/maezo/tools/workers/ans_submit.py`), `RECURSO-{tenant}-{numero_guia_tiss}-{glosa_id}` (`contas.py`), mais equivalentes em `inadimplencia.py`/`fraude.py`/`auth.py`/`recurso.py`; a convenção `{PREFIX}-{tenant}-{id}` está documentada em `mcp_cibseven/transport.py` — e NÃO a forma `{company_tenant_ref}:{workflow_type}:{workflow_business_ref}` que a proibição 6 congela; (b) as process-definition keys implantadas são `SP-OP-<DOMAIN>-<NNN>` (regex + 15 chaves congeladas em `src/maezo/tools/process_allowlist.py`), NÃO o prefixo `maezo-payer-` que a proibição 6 exige — ou seja, **toda BPMN implantada hoje viola a proibição 6 na forma**; (c) várias dessas business keys **embutem identificadores de registro de fonte** (`numero_guia_tiss`, `numero_contrato`, `prestador_id`), o que tensiona a proibição 5 ("nenhum ID cru de fonte em keys") de forma INDEPENDENTE do MZO-020. **AMENDED 2026-08-05 (orquestrador, verificação própria pós-gatekeeper) — a perna (c) é PIOR do que registrado e merece avaliação de privacidade em si:** `matricula_beneficiario` está no PRÓPRIO registro de PHI deste repo (`src/maezo/tools/workers/phi_vars.py`, frozenset `PHI_PROCESS_VARS`) e AO MESMO TEMPO é componente de business key CIB — `inadimplencia.py` faz `contrato = numero_contrato or matricula_beneficiario` → `CANCEL-{tenant_id}-{contrato}`, espelhado em `agents/fernando/graph.py`, com a `<bpmn:documentation>` citada como autoridade do esquema (é INTENCIONAL e especificado, não acidente). A proibição 5 proíbe PHI **em keys** literalmente, e business keys são persistidas no engine, usadas como chave de mensagem Kafka e entram em audit records — logo esta perna é violação DIRETA da cláusula, independente de log. VERIFICADO TAMBÉM: business keys SÃO logadas (`recurso.py`, 4 sítios `logger.warning(..., business_key=...)`) e passadas como `key=` ao publish; e NÃO existe rede de proteção — `LogScrubber` existe mas NÃO está ligado em `platform/observability.py::structlog.configure` (processors = TimeStamper / StackInfoRenderer / format_exc_info / ConsoleRenderer, sem scrubber; o docstring do próprio scrubber admite isso) e `PHI_FIELDS` do pseudonymizer cobre só `{cpf, nome, telefone, email}` — não cobre `business_key` nem `matricula_beneficiario`. **NÃO ESTABELECIDO, e registrado como limite honesto da verificação:** que uma key portando `matricula_beneficiario` chegue HOJE a uma linha de log — as keys logadas em `recurso.py` são `RECURSO-*`, que carregam `numero_guia_tiss`/`glosa_id` (IDs crus de fonte, problema da proibição 5 do mesmo jeito, mas não itens do registro de PHI); `inadimplencia.py`/`cancel.py` não logam as suas. Fechar essa questão exige varrer os 7 compositores de key do notification bridge. NADA foi alterado em produção. CONSEQUÊNCIA: as duas primeiras são divergências de FORMA (não de segurança) e reconciliá-las muda chaves de engine JÁ IMPLANTADAS — exige janela de dual-read/redeploy (a própria cláusula de rollback do MZO-020 prevê alias legado explícito, sem rewrite destrutivo) OU uma emenda ao ADR-0037 que aceite a forma viva. A terceira é substantiva e merece avaliação própria de privacidade. NADA foi alterado em produção nesta descoberta: nenhum worker, nenhuma BPMN, nenhuma allowlist. O MZO-020 implementa a forma do ADR (é o contrato aceito), e o gap fica REGISTRADO em `PLANS.md` §0.6 + aqui, com a migração de call sites explicitamente DIFERIDA. | Regra de não-fabricação: um agente não reescreve chaves de processo implantadas nem emenda um ADR aceito por conta própria; e não pode silenciar a divergência ao implementar a cláusula. Precedente ADR-0032/DL-0032 (ADR corrige ADR; achado documentado sem ação unilateral) + a cláusula de rollback do próprio MZO-020 (dual-read com alias legado). A tensão com a proibição 5 é anterior a este work package — está registrada aqui para não se perder, não como algo que o MZO-020 introduziu. | orquestrador (Fable 5), 2026-08-05 — decisão de reconciliação PENDENTE do dono |
| 2026-08-05 | DL-0042 | **PORTÃO DPO/LEGAL DO MZO-020 DESCARREGADO — aprovação humana explícita das semânticas de identidade portável (cláusula XRD-05 do ADR-0037); desbloqueia o MZO-020 e, por consequência, o MZO-050.** Aprovadores: **Lucas — Diretor Jurídico e de Compliance** e **Rodrigo — CEO e dono do repositório**, na sessão de orquestração de 2026-08-05. Base de atestação: declaração explícita do dono do repositório na sessão ("MZO-020 (identity — DPO/Legal) is approved by our legal and compliance director (Lucas) and by me (Rodrigo - CEO), register this and unlock the pending tasks") — o mesmo mecanismo de proveniência do DL-0040, e com a MESMA ressalva registrada honestamente: a aprovação de Lucas é **declarada pelo dono**, não uma assinatura individual do próprio Lucas neste repo; a capacidade de Lucas é **Jurídico + Compliance** (o mesmo papel que o `approval_record` do manifest AMH registra para LGPD/ANS), e nenhum papel intitulado "DPO" distinto foi nomeado — o portão é tratado como satisfeito pela autoridade do dono somada à do diretor jurídico, que é a leitura de "DPO/Legal" nesta organização. ESCOPO ESTRITO: descarrega o portão do **MZO-020 apenas**. **NÃO alcança** as aprovações **Médica, ANS e Security** do MZO-040 (`ActionExecutionGateway`), que seguem ABERTAS — nem qualquer outro gate §5 (stewards de inventário de fonte, replay DPO, regras de disposição). O DL-0040 permanece válido no que diz: a ratificação do ADR-0037 não fechou este portão; ele caiu por ato humano próprio e posterior. Nenhuma semântica de identidade foi inventada por agente: o MZO-020 implementa referências OPACAS validadas só na forma, sem parsing de significado, e rejeita explicitamente formatos de ID cru (CPF/CNS/`Patient/…`/`fhir:`). | Handoff do dono 2026-08-05 + a mensagem de aprovação citada acima; o gate existia por exposição LGPD nas semânticas de identidade (ADR-0037 XRD-05 "Cláusula gated por DPO/Legal"; DL-0040 §gates downstream). Precedente de proveniência: DL-0040 (ratificação do ADR-0037 pelo dono, papéis de aprovação exercidos e registrados). Registrar a ressalva da atestação declarada segue a mesma disciplina aplicada às ressalvas do `approval_record` do manifest AMH em DL-0041 — aprovação real, proveniência descrita como ela é. | Lucas (Diretor Jurídico e de Compliance) + Rodrigo (CEO/dono do repo), via orquestrador (Fable 5) |
| 2026-08-05 | DL-0041 | **XRG-3 FECHADO (orquestrador, Fable 5, 2026-08-05) — pin do consumidor Maezo escrito e verificado (MZO-010, branch `mzo-010-amh-contract-pin`), após preflight zero-trust de XRG-2 com as 9 condições do handoff do owner verificadas contra o REMOTO amh-data-platform.** XRG-2 constatado GENUINAMENTE fechado: manifest PUBLISHED @ AMH `1797244` (PR AMH #108), artefatos @ `09a0a282` (15 digests recomputados byte-exatos), publicação Glue real no run 30991849241 (não-dry-run; 3 SchemaVersionIds AVAILABLE conferidos nos logs), evidence artifact 8924396642 íntegro, `MAEZO_PUBLISH_ENABLED` removida pós-publicação, compat CI (schemas-validate) verde no PR de publicação. Decisões: (a) pin DUPLO-SHA no lock — `amh_commit_sha` `09a0a282` (fonte dos artefatos, declarado pelo manifest) + `amh_manifest_commit_sha` `1797244` (árvore com o manifest publicado) — porque o manifest publicado não pode viver no commit que ele mesmo pina; (b) fixtures AMH sintéticas VENDORIZADAS digest-gated em `tests/contract/amh/fixtures/` ≠ cópia de schema editável — proibição 4 do ADR-0037 respeitada (NENHUM `.avsc`/`.openapi` neste repo; bytes imutáveis: qualquer edição local falha o gate `verify-amh-contract-pin` E a suite); (c) PR #189 (dependabot cryptography 50.0.0, lockfile-only, 14 checks verdes) resolvido ANTES e SEPARADO do MZO-010 por instrução explícita do handoff — merged `dd03366`. Ressalvas de evidência AMH divulgadas (não-bloqueantes, registradas para o owner): 3 dos 4 approval_records do manifest são atestações DECLARADAS pelo CEO/owner na sessão de publicação (o próprio manifest o divulga; não são assinaturas individuais); o required-check server-side do main AMH é só "Security Gate" (o compat CI bloqueia por job-failure em PR + disciplina de merge, não por branch-protection context); `Schemas Sync` pós-merge do head AMH FALHOU em OIDC (`sts:AssumeRoleWithWebIdentity`) ANTES de qualquer chamada AWS — zero mutação de registry, sem drift, follow-up operacional do lado AMH. Gates §5 downstream INALTERADOS (DL-0040): DPO/Legal (MZO-020) e Médica/ANS/Security (MZO-040) seguem ABERTOS — nada nesta decisão os fecha. | Handoff do owner 2026-08-05 ("independently close XRG-3" após verificação; "resolve #189 independently"); ADR-0037 XRD-04 define o mecanismo do pin; XRG-3 por definição (ADR-042 AMH / ADR-0037) = verificação independente + pin exato + contract tests contra fixtures publicadas + evidence ID registrado — todos cumpridos no ledger row `mzo-010`. | orquestrador (Fable 5), 2026-08-05 |
| 2026-08-03 | DL-0040 | **ADR-0037 Proposed→Accepted por ratificação humana explícita do dono do repositório (Rodrigo) na sessão de orquestração de 2026-08-03.** A aceitação fecha a METADE MAEZO do gate XRG-1 do plano AMH-compat; o XRG-1 completo permanece PENDENTE do ADR companheiro (AMH-000) aceito no repo amh-data-platform. Os gates operacionais §5 downstream (DPO/Legal em valores de identidade/consent, Security em designs, stewards em inventário de fonte, aprovações Médica/ANS de MZO-040) permanecem exigíveis nas waves de implementação — esta ratificação não os fecha nem autoriza implementação de consumer (MZO-020..180 mantêm seus próprios gates). Paperwork: linha de Status do ADR-0037, blockquote de ratificação, row do README e este DL (mzo-000-ratify). | Precedente ADR-0027 (owner ratification); o override de ratificação autônoma registrado em DL-0039 foi RESPEITADO — quem moveu o Status foi o humano (repo owner, autoridade do stop point §5 do execution prompt), o agente só executou o paperwork sob instrução direta. | Rodrigo (repo owner) via orquestrador (Fable 5) |
| 2026-08-03 | DL-0039 | **DECISAO (orquestrador, Fable 5, 2026-08-03) — ADR-0037 DRAFTED como Proposed (MZO-000, Wave 0 do plano AMH-compat); aceitação = metade Maezo do gate XRG-1; HUMAN-GATED per docs/prompts/EXECUTE_AMH_COMPATIBILITY_PLAN.md §5 — nenhuma ratificação autônoma de agente/orquestrador move este Status (override explícito, para ESTE ADR, da autoridade autônoma de ratificação registrada em MEMORY/DL-0036).** O ADR-0037 (autor: boundary-adr-author, MZO-000): (a) ratifica pelo lado Maezo as doze decisões XRD-01..XRD-12 do plano técnico AMH-compat, todas na opção C; (b) SUPERSEDE PARCIALMENTE o ADR-0013 — morrem as cláusulas 1 (raw-CDC como contrato), 2 [apenas o wire dev-JSON], 3 (simulador-como-contrato → demovido a harness local de dev), 4 (credenciais Oracle NUNCA vêm ao Maezo) e 5 (DLQs cdc.* → tópicos `*.quarantine.v1` + inbox durável); SOBREVIVEM re-ancorados os princípios consume-not-duplicate, TASY-write-DROP e zero-infra-CDC (âncora de ADR-0019/0021/0014/0020 + 7 contratos de processo); (c) AMENDS ADR-0034 — a cláusula XRD-09 é o veículo de ratificação da re-introdução do chokepoint por chamada (`ActionExecutionGateway`), efetiva só com aceitação humana + evidência MZO-040; (d) fixa o catálogo canônico AMH-owned (6 artefatos, 3 tópicos, 3 quarentenas, baseline congelado do envelope), o pin imutável `config/integrations/amh/contracts.lock.json` (XRG-3) e as 6 proibições imutáveis da fronteira. Estado de gates na data (recon Wave-0): XRG-1/2/3 TODOS ABERTOS — AMH `main` @ `6348cb0` sem ADR de ownership, sem `schemas/{avro/integration,openapi,contracts}/`, sem manifest/evidence-ID; Maezo sem `config/integrations/`; drift AMH-side registrado (commit `8a3a061` removeu o sufixo shell non-blocking "or-true" do step de compatibilidade Avro, tornando o gate BLOQUEANTE — premissa do AMH-040 parcialmente pré-satisfeita, só paths avro, sem publicação Glue/manifest). Nenhuma claim de prontidão ou aceitação é feita. | MZO-000 é o único work package da Wave 0 e pré-requisito de TODO o resto (mapa de gating no próprio ADR): o conflito de contrato provado no forensics summary (Maezo espera views `cdc.amh.tasy.*`, AMH shippa tabelas físicas `cdc.austa_clinicas.tasy`) torna qualquer consumer sobre o ADR-0013 fabricação; o §5 do execution prompt lista "accepting/superseding an ADR or changing capability/source ownership" como stop point humano obrigatório, o que para ESTE ADR sobrepõe a prática de ratificação autônoma; precedente DL-0035 (ADR proposto para ACELERAR a decisão humana, não substituí-la — drafted-pending-ratification). | orquestrador (Fable 5), 2026-08-03 |
| 2026-07-27 | DL-0038 | **DECISAO (orquestrador, Fable 5, 2026-07-27) — postura fail-closed vs best-effort dos topicos de notificacao, resultado de auditoria (construcao em andamento no branch `tier2-notify-integrity`).** FLIP para FAIL-CLOSED: `lgpd.request_additional_proof`, `lgpd.send_response`, `recurso.notify_sla_risk`, `recurso.submit_appeal` — hoje propagam a falha de publish CRUA, sem fronteira (boundary) declarada; passam a exigir confirmacao de entrega antes de completar a task. MANTIDO best-effort POR DESENHO (nao e defeito): `ans_submit.notify_regulatorio` (notificacao ADVISORY — a User Task regulatoria e criada pelo engine independentemente do resultado da notificacao; a variante de deadline-risk ja e duplo-publicada por uma perna propagadora separada; fail-closar o caminho PRINCIPAL de submissao ANS por causa de uma notificacao advisory criaria dano de PRAZO REGULATORIO, pior que o gap que se busca mitigar) e `recurso.track_status` (loop de re-publish P5D — best-effort e a semantica correta de um heartbeat periodico). Adicionalmente: (a) CORRECAO de falso-sucesso — `AioKafkaEventsProducer.publish`/`_publish_one` passam a RETORNAR o resultado real de entrega (`bool`); `event_published` para de MENTIR quando o publish primario falha em silencio numa rota best-effort (antes: `True` fabricado independente do resultado real); (b) fail-closed ESCOPADO POR TIPO DE EVENTO para `nip.handoff_ans_submit` — o token so e liberado quando a entrega e CONFIRMADA (durabilidade sem outbox transacional; idempotencia da ponte/bridge provada); (c) ancora `tenant_id` adicionada aos 7 predicados da ponte (bridge), fechando um gap de isolamento multi-tenant nos 7 pontos de decisao. | Auditoria da postura real de cada topico contra o contrato do processo (`spec/processes/bpmn/**`) partindo do principio de que uma notificacao cujo UNICO efeito de negocio e notificar nao pode mentir sobre seu proprio resultado (`event_published`); fail-closed onde a falha de entrega tem consequencia juridica/PHI direta (LGPD DSR, recurso), best-effort preservado onde fail-closar criaria um dano REGULATORIO PIOR (prazo ANS) do que o gap de notificacao que se busca fechar. Fundado na arvore auditada em `tier2-notify-integrity` (`events_kafka_producer.py`, `harness.py`) + precedente DL-0031 (LGPD fail-closed) e DL-0034 (escalation notify raw-handler). | orquestrador (Fable 5), 2026-07-27 |
| 2026-07-27 | DL-0037 | **DECISAO (orquestrador, Fable 5, 2026-07-27) — postura de degradacao da delegacao A2A dos workers de dossie cred/adequacao (construcao em andamento no branch `tier2-dossier-a2a`).** Quando o `DelegationDispatcher` fail-closed-assinado estiver disponivel, `operadora.cred.prepare_dossier` e `operadora.adequacao.prepare_remediation_dossier` DELEGAM via A2A (envelope assinado) para Carolina/Andre; na AUSENCIA ou FALHA do dispatcher, o worker faz FAIL-NEUTRAL-COM-GAP-DISCLOSED — retorna `{"dossier_prepared": False, "dossier_gap": <razao>}` (nunca fabrica um dossie, nunca bloqueia o fluxo) — porque o contrato A2A fixa "instrui, nao decide": a User Task humana DEVE sempre abrir, com ou sem dossie. Isto ESTENDE (nao reverte) a postura stub-local de DL-0033 (BUILT 2026-07-26, t5-workers-f2): os stubs locais `FunctionWorker` continuam como o degradado-padrao sem dispatcher, mas o caminho de delegacao REAL (`DelegationEnvelope` + `DelegationDispatcher.delegate`) passa a ser ativado quando disponivel, em vez de permanecer permanentemente deferido. Andre desambigua o `task_type` compartilhado `analytics.population` mapeando `envelope.origin` → fluxo (pagto vs adequacao) — sem alterar `agent.yaml`/`accepted_task_types` nem nenhum card A2A. O schema de campos do dossie exibido na UT permanece GATED por SME/PO (marcado como ponto de injecao explicito, nao inventado aqui). **EMENDA 2026-08-03 (item-9 wave-4.6; achado 9 do gatekeeper de dossie — residuo de documentacao):** o corpo original enumera APENAS os dossies `cred`/`adequacao`, mas a MESMA semantica fail-neutral-com-gap-disclosed vale tambem para `operadora.pagto.prepare_approval_dossier` (dossie de aprovacao de alcada; delegacao REAL para Andre via o `task_type` compartilhado `analytics.population`, desambiguado por `envelope.origin` para o fluxo `pagto_dossier` — exatamente o mecanismo que o corpo original ja descreve). Dispatcher ausente/falho/rejeitado, ou identificadores de negocio faltando, retorna `{"dossier_prepared": False, "dossier_gap": <token limitado>}` (`dispatcher_unavailable` | `missing_business_identifiers` | `delegation_timeout` | `delegation_failed` | `delegation_rejected:<motivo>`) com log LOUD e a external task COMPLETADA: `UT_AprovacaoAlcada` SEMPRE abre, com ou sem dossie, e o aprovador humano ve o gap. Um dossie entregue porem degradado retorna `dossier_prepared=True` + `dossier_gap="degraded:<token>"` — nunca um sucesso limpo que esconda a degradacao. Correcao de ESCOPO datada, nao reescrita silenciosa do corpo original: as bordas `cred`/`adequacao` aterrissaram em #178 e a borda `pagto` (a ultima das tres) na cadeia item-9 wave-3 (`d5e7571`), ainda NAO em `main` na data desta emenda. O gate SME/PO sobre o schema de campos do dossie permanece inalterado e vale igualmente para o dossie de pagamento. | Os contratos A2A (`docs/design/A2A-dispatcher-card-signing.md`) e o precedente ADR-0032/DL-0033 ja estabelecem que dossies sao HITL-instrutivos, nunca decisorios; fail-neutral-com-gap preserva a garantia arquitetural (ADR-0005 HITL) sem bloquear a UT quando o dispatcher esta indisponivel, mantendo o invariante "a UT humana sempre abre" em ambos os caminhos. Decisao de postura do orquestrador; construcao delegada ao branch `tier2-dossier-a2a` (nao mergeado nesta sessao). | orquestrador (Fable 5), 2026-07-27 |
| 2026-07-27 | DL-0036 | **ADR-0025 / ADR-0026 / ADR-0028 / ADR-0030 RATIFICADOS (orquestrador, Fable 5, 2026-07-27) — batch Proposto→Accepted; substancia ja implementada e independentemente R1-verificada, so o paperwork (Status line + README + DL row) estava atrasado.** **ADR-0025** (PEP↔Policy Unification, T1.8) → Accepted: implementacao completa e R1-verificada (`pep.py:71,287,317`; `docs/evidence-ledger.md:35,43`, design+implementacao verificadas, merged #32/#36); **amendment note:** Q2 (§6, `analise_recurso` orphan) permanece EM ABERTO — a acao segue ausente de `L0-core.yaml` (grep 0 hits) — a ratificacao NAO resolve Q2, so confirma D1-D6. **ADR-0026** (Worker standardization, T1.2/B13) → Accepted: implementacao completa e R1-verificada (`base.py:243` FunctionWorker, `bootstrap.py` register_all_workers; `docs/evidence-ledger.md:40,53`, "B13 closed, 16/16 registered 99 topics, merged #52"); **amendment note:** contagem de bootstraps corrigida de 16 para **17** — T3.1 "events.publish fix" (`docs/evidence-ledger.md:66`) adicionou um 17o bootstrap raw-handler (`register_events_workers`, NAO FunctionWorker-wrapped), correcao datada aqui, nao reescrita silenciosa do corpo original. **ADR-0028** (DMN engine-side, T1.4/T1.5) → Accepted: unico dos quatro onde o ledger JA dizia "RATIFIED" sem DL correspondente (`docs/evidence-ledger.md:46`, "ADR-0028 verified + RATIFIED Accepted (merged #46)"; T1.5 cutover `:54` confirma golden parity 75/75 live) — este e o backfill da linha de DL que faltava, nao uma nova decisao. **ADR-0030** (Worker error semantics vs BPMN boundary, T3.1) → Accepted: design R1-verificado PASS em 2 rounds (`docs/evidence-ledger.md:95`); Tier-0 construido e R1-verificado (`:98`); **amendment note:** migracao permanece FASEADA — Tier-2 remanescente (`cred`/`nip`/`pagto`/`programa`/`reembolso`, sem `*_BPMN_ERROR_ALLOWLIST` importado em `service.py`) e os enablements Tier-3 permanecem FOLLOW-UPS explicitos, nao dobrados silenciosamente em "Accepted" — Tier-3 dividido por familia: `cred` guard-blocks com migracao raise-side FEITA (PR #166/`0ecacbb`: `credenciamento.py:337,395` levantam `WorkerBpmnError(ERR_DECRED_NOT_HUMAN)`/`(ERR_CRED_DENIAL_NOT_HUMAN)`), pendente APENAS o enablement da allowlist de producao (T-E-deferred per ADR-0030 §4); `inadimplencia` NAO migrado (`inadimplencia.py:474` segue no coded exception simples, zero migracao); T-E aterrissou (`:116`) mas essa mesma linha declara ZERO allowlist enablements Tier-3 — runtime de todos os codigos Tier-3 segue audited-incident, inalterado; o row DL-0030 e atualizado in-place (nao duplicado) com uma clausula RATIFICADO, per o precedente amend-in-place de DL-0034. **ADR-0029 permanece Proposed — FORA deste batch**: seu Status line exige ratificacao do DPO (gate humano/externo), nao apenas do orquestrador; nada foi alterado no arquivo. | Todas as quatro implementacoes ja tem verificacao adversarial R1 independente na arvore (`docs/evidence-ledger.md:35,40,43,46,53,54,66,95,98,113,116`); tres delas (0025/0026/0030) ja eram tratadas como Accepted por decisoes downstream (DL-0034 cita "ADR-0026 §2"; DL-0030 refina "ADR-0026 §5") sem o flip formal ter ocorrido — a unica pendencia era o passo de governanca, nao engenharia adicional. Fundado em `scratchpad/recon/recon-e.md` (recon E, 2026-07-27), re-verificado citacao-a-citacao contra `docs/evidence-ledger.md` nesta sessao. | orquestrador (Fable 5), 2026-07-27 |
| 2026-07-25 | DL-0035 | **ADR-0033 PROPOSTO (identidade de servico do agente — emissao/verificacao do certificado do T-G) — pendente ratificacao do arquiteto de seguranca/infra.** Documento produzido para ACELERAR a decisao humana, nao para substitui-la: estrutura o espaco de decisao (drivers, 3 opcoes com trade-offs, perguntas abertas) para a metade certificado/service-account de T-G que ADR-0007:10 pede numa unica clausula sem elaborar mecanismo de emissao/verificacao — DESIGN-GAP ja sinalizado por ADR-0032:67-73 e por `docs/design/A2A-dispatcher-card-signing.md` §4/§7.1 ("This needs a NEW ADR before any cert-issuance code is written"). Opcoes cobertas: (A) SPIFFE/SPIRE workload identity + mTLS; (B) Vault-issued short-lived service-account JWT por agente+tenant+agent_version (RECOMENDADA nao-vinculante); (C) certificado estatico por agente de CA interna. Recomendacao funda-se em infra REAL verificada nesta sessao: `deploy/terraform/modules/` nao tem `kms`/`acm`/`pca` hoje; o unico seam de "vault" existente e ESO+AWS Secrets Manager autenticado por IRSA (`externalsecret.yaml:2,11`); ServiceAccount ja e per-agente via IRSA (`deployment-agent-runtime.yaml:8`) mas e identidade de nuvem, nao de aplicacao; nenhum service mesh (Istio/Linkerd/SPIFFE/SPIRE) esta implantado. Caminho de migracao interim->cert-backed documentado (AUDIT_AGENT_ID="operadora-worker", `harness.py:111`, continua funcionando sem mudanca ate ratificacao+provisionamento externo). NAO EDITA ADR-0007 — **amends** (elabora a clausula `:10`), nao supersede, seguindo o precedente ADR-0027/ADR-0032 (`docs/adr/README.md:8`). Complementa (nao bloqueia nem e bloqueada por) a metade Agent-Card-assinado de T-G ja construida em `t2.4-a2a-w1-card-signing` (branch separada, nao mergeada) nem T-F (delegacao A2A, roda sobre `emit_once`, W4) — ambas ORTOGONAIS a este ADR. BUILD real da metade cert fica bloqueado ate (a) ratificacao humana e (b) vault/KMS/CA provisionado (dependencia externa). Sessao DOCS-ONLY: nenhuma edicao de `src/`/`tests/`/`spec/`/evidence-ledger; esta e arquitetura NAO-TASKEADA (nao fecha nenhum `T<fase>.<n>` do backlog), por isso nao carrega trailer `Task:` no commit nem linha no evidence-ledger. | Brief orquestrador (T-G e a unica peca A2A/audit-identity genuinamente ADR-gated per ADR-0032 + a recon A2A); fundado em leitura direta de ADR-0007/0032, `A2A-dispatcher-card-signing.md`, `src/maezo/a2a/{card,signing,registry,assembly}.py` (branch W1), `AUDIT_AGENT_ID`/`AgentDecisionProvenance` (grep+leitura), `docs/adr/README.md` (convencao) e a infra real (`deploy/terraform/modules/`, `deploy/helm/maezo-tenant/templates/`). Ratificacao e do arquiteto de seguranca/infra + gatekeeper, nao desta sessao. | docs-adr-agent (T-G identity ADR) |
| 2026-07-24 | DL-0034 | **ACEITO — ratificado pelo orquestrador 2026-07-26; construido em t5 (branch `t5-workers-f1`). Originalmente PROPOSTO na T2.9 SME-packaging pass.** Workers de notificacao da familia escalation (`operadora.escalation.notify_team`/`notify_supervisor`/`notify_fallback`, `SP-OP-ESCALATION-001`) devem publicar worker-side via RAW ASYNC KAFKA HANDLERS (mesma forma sancionada do #55 R-B `operadora.lgpd.request_additional_proof`/`events.py`) quando forem religados — isto NAO e uma excecao a "pureza dict-first" do ADR-0026 §2, e a forma CORRETA para esta classe de tarefa: para escalation, notificar E o proprio efeito de negocio (nao ha calculo de dominio algum alem de rotear+notificar), e o processo nao modela estes 3 external tasks como o `operadora.events.publish` generico — sao topicos PROPRIOS. Evidencia: `NotifyTeamWorker`/`NotifySupervisorWorker` sao `WorkerBase` (sync, dict-first) e `register_escalation_workers` faz `del kafka, seams  # unused` (`escalation.py:199`) — por design ADR-0026 §2 uma classe `WorkerBase` nao tem seam Kafka async utilizavel dentro de `execute()`, entao a notificacao NUNCA acontece de fato (so um dict `{"status": "teams_notified", ...}` e retornado). `tests/integration/processes/test_sp_op_escalation_001.py:91-115` (`_NOTIFY_KAFKA_GAP_REASON`) documenta isto como gap confirmado AO VIVO (CIB Seven 2.1.0): `probe.notified_teams`/`notified_supervisors` NUNCA observam uma execucao e `ERR_ESC_NOTIFY_FAILED`/o boundary de fallback NUNCA disparam (nada chama `kafka.publish` para estes tipos). A tabela de 24 boundary catches do ADR-0030 ja classifica `ERR_ESC_NOTIFY_FAILED` como G2-fs (fail-safe tecnico) com "Worker raises it today? NO (Kafka/notify gap)" — o MESMO defeito, ja agendado para o Tier 1 da migracao ADR-0030 ("entangled with the Kafka-producer gap ... co-schedule"). Precedente ja sancionado e ao vivo: #55 R-B (ADR-0031 decisao 4) e um raw handler async registrado via `harness.register(topic, handler)` (nao `WorkerBase`) exatamente porque seu unico efeito e notificar, publicando no MESMO topico interno `operadora.notifications.internal` que escalation usaria (`lgpd.py:475` vs `escalation.py:84`). Este DL fixa a FORMA (raw handler) a adotar quando a implementacao real acontecer (tarefa de build futura, fora do escopo desta sessao doc-only); nao autoriza nem agenda o build em si. **AMENDED 2026-07-25 (pos-merge `origin/main` 6288341):** (a) o topico morto `operadora.escalation.notify_fallback` e seu `NotifyFallbackWorker` foram DELETADOS pelo #133 (t2.5-p2b-round2 — zero `camunda:topic` correspondente em `spec/`, ver `escalation.py:152-157`); este DL passa a cobrir APENAS `notify_team`/`notify_supervisor`. (b) Citacoes re-pinadas: `del kafka, seams` agora em `escalation.py:168`; `_NOTIFY_KAFKA_GAP_REASON` em `test_sp_op_escalation_001.py:107-119` (xfails em `:399/:469/:513`); a citacao original '`escalation.py:84`' referia-se na verdade ao arquivo de TESTE (`test_sp_op_escalation_001.py:84`, `_NOTIFICATIONS_TOPIC`), e `lgpd.py:475` agora e `lgpd.py:405`. (c) Substancia INALTERADA e re-verificada pos-merge: os dois notify workers seguem `WorkerBase` sem nenhum `kafka.publish` (grep 2026-07-25). **BUILT 2026-07-26 (t5-workers-f1):** os dois notify workers foram convertidos de `WorkerBase` para RAW ASYNC KAFKA HANDLERS — `make_notify_team_handler`/`make_notify_supervisor_handler` registrados via `harness.register(topic, handler)` (`escalation.py`), publicando as notificacoes `escalation.notify_team`/`escalation.notify_supervisor` em `operadora.notifications.internal` (kafka=None fail-closed: completa a task + log alto, NUNCA fabrica publish nem trava o fluxo ate `UT_TratarEscalonamento`), espelhando #55 R-B EXATAMENTE; `register_escalation_workers` agora threada `kafka` (nao mais `del kafka`). O raise do modelo `ERR_ESC_NOTIFY_FAILED` (p/ os boundaries de fallback `BE_FalhaNotificacao`/`BE_Notif*`) fica FORA de escopo deste build — permanece ADR-0030 Tier-1 (co-agendado com o gap do produtor Kafka), consistente com "mirror R-B" (R-B nao levanta bpmnError). Testes unitarios reescritos (`test_escalation_notify_team.py`, handlers async + FakeKafkaPublisher); `raw_handler_topics` de `test_bootstrap_registration.py` atualizado (notify_team/notify_supervisor saem do WorkerRegistry). FLAG p/ re-flip com engine ao vivo: os xfails de observabilidade de notificacao em `test_sp_op_escalation_001.py` (`:399`/`:513`, `_NOTIFY_KAFKA_GAP_REASON`) passam a XPASSar sob engine real e precisam de remocao do marker com prova ao vivo (fora do gate unit desta sessao); os xfails de fallback (`:469`) permanecem validos (dependem do raise ERR_ESC_NOTIFY_FAILED Tier-1, nao construido). | Sessao de packaging SME (T2.9, `docs-p2b-escalation-packages`) — achado ao auditar #55 R-B/R-F/R-G para os pacotes DPO/médico-auditor; fundado em escalation.py/ADR-0026/ADR-0030/ADR-0031 + o teste de integracao ao vivo. Ratificacao e do orquestrador, nao desta sessao. | sme-packaging-agent (T2.9) |
| 2026-07-24 | DL-0033 | **ACEITO — ratified by orchestrator 2026-07-26, built in t5 (branch `t5-workers-f2`).** (Originalmente PROPOSTO na T2.9 SME-packaging pass; a frase "NAO ratificado ... requer aprovacao do orquestrador antes de qualquer build" abaixo fica SUPERSEDED por esta ratificacao.) Os workers de dossie A2A Carolina/Andre (`operadora.cred.prepare_dossier`, `operadora.pagto.prepare_approval_dossier`, `operadora.adequacao.prepare_remediation_dossier`) devem ser construidos como STUBS LOCAIS (`FunctionWorker`, echo/log informativo, SEM `DelegationDispatcher`), espelhando o precedente `programa.enroll_beneficiario` (L3, neutro, "enrollment is not an adverse effect" — `programa.py:77-95`) — a INTEGRACAO A2A REAL (`DelegationEnvelope` + `DelegationDispatcher.delegate` ate Carolina/Andre) fica DEFERIDA a uma tarefa futura de wiring-A2A completo (rotulada "M5" no brief orquestrador; nota de drift: o repo ja tem um marco historico chamado M5 — "feat(M5): Agent Framework + A2A + ..." per `docs/reports/T0.3-agent-yaml-drift.md:16` — este DL NAO reabre aquele marco passado, apenas usa o mesmo rotulo do brief para uma tarefa de fechamento futura; a orquestracao deve confirmar/renomear o rotulo ao agendar). Racional: hoje os tres topicos sao GAPS confirmados sem NENHUMA funcao implementadora, nao apenas "delegacao parcial" — `credenciamento.py:296-297`, `pagto.py:376-377`, `adequacao.py:266-267` listam os tres, respectivamente, no comentario "Spec topics with NO implementing function today (gap, not fabricated here)"; `andre/graph.py:12,30` e o `agent.yaml`/`accepted_task_types` de Carolina ja declaram os `task_type` recebedores (`pagto_dossier`/`adequacao_dossier`; `credentialing.analyze`) mas NENHUM call site origina o envelope (grep confirmado); `docs/reports/predeploy-findings.json` ja cataloga cada um destes como achado disclosed (GAP prepare_dossier de CRED sem `DelegationDispatcher`; RECURSO sem parametro `dispatcher`; INADIMPLENCIA→Fernando faltando o kwarg `dispatcher=` na wiring de producao). Construir um stub local AGORA fecha o orfanato de topico BPMN (as User Tasks humanas hoje abrem SEM dossie algum) sem inventar logica de negocio-de-delegacao antes do dispatcher real estar pronto — mesmo raciocinio que motivou o precedente `enroll_beneficiario`. NAO ratificado — apenas recomendacao desta sessao de packaging; requer aprovacao do orquestrador antes de qualquer build. **AMENDED 2026-07-25 (pos-merge `origin/main` 6288341):** os tres topicos SEGUEM gaps mesmo apos #130 (cred family reconciliation) e #133 (pagto/adequacao round-2) — comentarios de gap re-pinados: `credenciamento.py:437-438` ('Carolina A2A integration, separately gated'), `pagto.py:550-551` e `adequacao.py:348-349` ('Andre A2A-gated — out of scope for t2.5-p2b-round2'); os proprios builders da round-2 deferiram exatamente estes tres, corroborando a direcao deste DL. `enroll_beneficiario` permanece em `programa.py:77-95` (citacao inalterada). **BUILT 2026-07-26 (t5-workers-f2):** os tres stubs locais foram construidos e registrados exatamente como este DL prescreve — `credenciamento.prepare_dossier` (`operadora.cred.prepare_dossier`), `pagto.prepare_approval_dossier` (`operadora.pagto.prepare_approval_dossier`), `adequacao.prepare_remediation_dossier` (`operadora.adequacao.prepare_remediation_dossier`) — cada um um `FunctionWorker` neutro (log + `{"dossier_prepared": True, ...}`, SEM `DelegationDispatcher`), espelhando `programa.enroll_beneficiario`; os tres comentarios de gap "NO implementing function today" viraram entradas de topic-mapping; a INTEGRACAO A2A REAL (Carolina/Andre `DelegationEnvelope`) segue DEFERIDA (tarefa futura de wiring-A2A). Testes unitarios espelhando `test_enroll_beneficiario` + guardas de registro atualizadas (`test_{credenciamento,pagto,adequacao}.py`). | Sessao de packaging SME (T2.9, `docs-p2b-escalation-packages`) — achado ao auditar o estado real dos tres workers de dossie contra `docs/review-queue.md` GAP-ADEQ-4 (cujo texto descreve `prepare_remediation_dossier` como ja delegando a Andre via `DelegationDispatcher` — DIVERGE do codigo atual de `adequacao.py`, que ainda lista o topico como gap; flagged como drift de documentacao, nao corrigido aqui pois `review-queue.md` foi editado apenas com um append minimo por esta mesma sessao, nao uma reconciliacao retroativa). Ratificacao e do orquestrador, nao desta sessao. | sme-packaging-agent (T2.9) |
| 2026-07-24 | DL-0032 | **ADR-0032 RATIFICADO (orquestrador, 2026-07-24) — corrige a alegação de "runtime completo" do ADR-0015 (é do donor v1 PR #17, não do v2).** ADR-0015 (Accepted 2026-06-13) afirma em `:14` "A PR #17 implementou o runtime completo em `src/maezo/a2a/`" e descreve `dispatcher.py`/`delegation.py`/`facts.py` como se existissem em v2. Reverificado 2026-07-24 contra `main=1318e1b`: `src/maezo/a2a/` tem só 4 arquivos (`__init__.py`/`anti_loop.py`/`card.py`/`registry.py`, único commit tocante `7e76006`); `git log --all --diff-filter=A` para `dispatcher.py`/`facts.py`/`delegation.py`/`idempotency.py` retorna ZERO em todo o histórico — nunca existiram neste repo; `AgentCard`/`A2ARegistry`/`AntiLoopGuard` têm ZERO call site de produção (só docstrings de gap em `rafael/graph.py:48-50`, `beatriz/graph.py:89-90`, `marina/graph.py:115`); `PLANS.md:43` já rastreia "A2A dispatcher + card-signing NÃO feitos (backlog P3)"; DL-0009 (contemporâneo à autoria do ADR) já registrava "ADR-0015 formaliza runtime A2A de PR #17" sem distinguir donor/v2. **Amends (não supersede) ADR-0015**, per `docs/adr/README.md:8` + precedente ADR-0027/DL-0026 (nova ADR corrige, arquivo original intocado — histórico é histórico). Consequências: T-F (auditoria de delegação A2A, `docs/design/audit-emit-path-wiring.md:387-388,627-628`) e a metade signed-Agent-Card de T-G (`:389-391,629`) ficam BLOCKED-ON este substrato — sem dispatcher não há chokepoint pra instrumentar ou verificar; a metade service-account/cert de T-G é um DESIGN-GAP próprio (ADR-0007 só tem uma cláusula em `:10`, sem mecanismo de emissão nem chokepoint de verificação) que precisa de ADR dedicado, fora do escopo desta emenda. Aponta para `docs/reports/T2.4-a2a-agent-card-signing-gap.md` (2026-07-17), que chegou à mesma conclusão pro signing. ZERO edição de `src/`/`tests/`/`spec/`/evidence-ledger; `make validate-artifacts`/`make validate-signoff` verdes. | Brief orquestrador (correção de status ADR-0015 ground-truth); reverificação direta da árvore nesta sessão (git log/grep + leitura de PLANS.md/ADR-0007/ADR-0027/DL-0009/T2.4-report) — nenhuma citação aceita sem re-pin. Ratificação é do orquestrador/gatekeeper, não do autor (ground rule 2) | docs-agent (R2, correção ADR-0015) |
| 2026-07-19 | DL-0031 | **ADR-0031 PROPOSTO (LGPD DSR identity gate fail-closed) — pendente ratificacao DA POLITICA DE PRODUTOR pelo DPO.** Decisao: `ValidateIdentityWorker` confirma identidade SO com sinal explicito `identidade_verificada is True` (ausente/False/lixo -> NAO confirmada -> challenge sub-flow); a mera presenca do `titular_pseudo_id` (OBRIGATORIO) NUNCA confirma (fecha o defeito fail-OPEN A3). GAP-LGPD-6: `titular_pseudo_id` vazio -> **RAISE** `WorkerBpmnError(ERR_DSR_IDENTITY_UNVERIFIED)` (antes retornava o codigo como VALOR de dict, deixando `BE_IdentidadeInverificavel`/`End_IdentidadeInverificavel` inalcancaveis) — guard TECNICO, NUNCA acusacao de fraude (L0 `fraud_accusation` intacto); `max_retries=1` (guard deterministico, espelha auth). `ERR_DSR_IDENTITY_UNVERIFIED` e consumption-covered + NAO-adverso -> Tier-0 (unido em `service.py` via `LGPD_BPMN_ERROR_ALLOWLIST`, NAO T-E-gated). Construido o worker #55 R-B `request_additional_proof` (raw handler async; SO pede prova, NUNCA seta `identidade_verificada`) para o challenge nao travar em prod. Dois produtores ratificados do sinal (seed de canal autenticado DPO-ratificado; correlacao humana `msg.lgpd.proof_received` que reverifica) — **so a POLITICA DE PRODUTOR** (quais canais/quais provas) fica pendente do DPO; a semantica CONSUMIDORA (fail-closed) nao espera por isso. ZERO edicao de BPMN/DMN (o modelo ja tinha boundary + loop de reverificacao). 4 strict-xfails T3.1 flipados (boundary/expira/pede-prova/prova-recebida), cada um provando a propriedade fail-closed em engine real. Fundado em LGPD Art. 18/11/19 II + o threat model anti eng.-social do proprio processo (`bpmn:87`, `contract:11`), conformante com ADR-0030. | Brief orquestrador-ratificado (design a6722411, Opcao C); fundado na arvore (`main=a86195e`) apos reverificacao de lgpd.py/service.py/harness/base + boundary-proof gate + BPMN. Ratificacao e do orquestrador/gatekeeper; a politica de produtor e do DPO (rule: nao inventar politica de identidade sem sign-off). | lgpd-identity-impl-engineer (T2.8) |
| 2026-07-18 | DL-0030 | **ADR-0030 ACEITO — ratificado pelo orquestrador (Fable 5) 2026-07-27, registrado em DL-0036. Originalmente PROPOSTO em 2026-07-18 (T3.1) — pendente ratificacao.** Decisao: **Opcao A — `WorkerBpmnError` modelado**, com a linha modelado-vs-incidente tracada EXATAMENTE na declaracao do boundary catch no spec: um worker levanta `WorkerBpmnError(code)` sse-e-somente-se `code` for um `bpmn:error@errorCode` declarado num error boundary event anexado ao external task daquele worker com COBERTURA DE CONSUMO — declarado em todo processo consumidor, OU restrito aos processos declarantes por condicao de dispatch worker-side verificada mecanicamente contra o spec (caso `operadora.events.publish`/`ERR_EVENT_PUBLISH_FAILED`: filtro events.py:120-127, gate :259 — F2) — provado pelo boundary-proof gate (clausula (c) faseada warn→hard durante a migracao, F5); todo o resto (falhas inesperadas, input mau/imutavel, guards L0 SEM boundary modelado) permanece incidente-fail-closed. Correcao do brief fundada na arvore: o mecanismo `WorkerBpmnError`+allowlist JA EXISTE (harness.py:129,916-943) e auth/cancel/events ja o levantam; o bloqueador sistemico real e a allowlist de producao VAZIA (`service.py:300 frozenset()`) + o boundary-proof gate NAO-construido, nao a reclassificacao para `ValueError` (que so afeta excecoes de dominio codificadas, base.py:284-294). **24 boundary catches em external tasks enumerados** em 12 familias (auth/ans/cancel/cred×3/escalation×8/inadimplencia/lgpd/nip×3/pagto/programa/recurso×2/reembolso; adequacao/contas/fraude tem ZERO). Refina ADR-0026 §5 (gate promovido de follow-up a pre-condicao); agenda T-E (audited-refusal) como co-requisito duro para TODOS os codigos `*_NOT_HUMAN` + o denial-block — incl. `ERR_CANCEL_MANTER_NOT_HUMAN` do G1 (F4: ativar um guard pre-T-E trocaria incidente humano-visivel por fim limpo silencioso) — sem contradizer o deferral atual do audit-emit design. Opcao B (podar os catches) REJEITADA (deletaria 24 caminhos de seguranca nao-adversos modelados; ADR-0029 e poda de audit_chain, nao de BPMN). DOCS-ONLY; nenhuma edicao de src/spec/test. **AMENDED 2026-07-18 por verificacao adversarial R1 round 1 (F1-F6):** evidencia de xfail CORRIGIDA — os 19 strict-xfails das 3 suites **phase-1** sao o Kafka-producer gap (sub-claim exata), mas as 13 suites **phase-2** (`t3.1-process-suites-phase2-land` @ d27264e) carregam **≥11 (13 incl. o par composto cred guard-shape `_CRED_GUARD_SHAPE_MISMATCH_REASON` ×2, cujos testes o autor rotulou pelo bloqueador PRIMARIO de worker faltante — mesma classe por mecanismo secundario identico, correcao de contagem por reverificacao R1) strict-xfails documentando EXATAMENTE esta classe** (programa×2/nip×3/cred×1/lgpd×1/recurso×2/ans_submit×2/cred-guard-shape×2) — 11 FLIPAM nos Tiers 1-2, o par composto no Tier 3 (xfail→XPASS→marker removido com prova live) — evidencia que FORTALECE a Opcao A; pares (topic,errorCode) distintos corrigidos 15→17 (15 = errorCodes distintos); `events.py:193 ERR_PUBLISH_MISSING_TOPIC` catalogado (cleanup Tier-0). **RATIFICADO 2026-07-27 (orquestrador, Fable 5; ver DL-0036):** ADR aceita — design R1-verificado PASS (`docs/evidence-ledger.md:95`) e Tier-0 construido e R1-verificado (`docs/evidence-ledger.md:98`, allowlist de producao populada a partir do gate). A migracao permanece FASEADA e nao 100% completa na ratificacao: Tier-1 (`ERR_ESC_NOTIFY_FAILED`) construido (DL-0034 "BUILT 2026-07-26"), mas o proprio raise segue fora de escopo, permanece Tier-1; Tier-2 completo para `lgpd`/`recurso` (`docs/evidence-ledger.md:113`), mas as familias remanescentes `cred`/`nip`/`pagto`/`programa`/`reembolso` ainda NAO tem `*_BPMN_ERROR_ALLOWLIST` importado em `service.py`; Tier-3 dividido por familia: **`cred` guard-blocks — migracao raise-side FEITA** (PR #166, commit `0ecacbb`: `credenciamento.py:337` levanta `WorkerBpmnError(ERR_DECRED_NOT_HUMAN)` e `:395` levanta `WorkerBpmnError(ERR_CRED_DENIAL_NOT_HUMAN)`), pendente APENAS o enablement da allowlist de producao (ambos os codigos T-E-deferred per ADR-0030 §4 — sem `CRED_BPMN_ERROR_ALLOWLIST` importado em `service.py`); **`inadimplencia` contract-suspension — NAO migrado** (`inadimplencia.py:474` segue levantando o coded exception simples `InadimplenciaError(ERR_CONTRACT_SUSPENSION_NOT_HUMAN)`, zero migracao) — T-E (audited-refusal) ja aterrissou (`docs/evidence-ledger.md:116`) mas essa mesma linha declara "ZERO allowlist enablements" para os codigos Tier-3 `*_NOT_HUMAN`: o comportamento em runtime de todos os codigos Tier-3 segue o caminho audited-incident, inalterado; enablements permanecem follow-ups per-familia. Ratificar a DECISAO nao implica migracao completa. | Brief T3.1 (process-engine-architect R1); fundado na realidade do repo apos verificacao de harness/base/service + enumeracao BPMN + evidencia de xfail phase-1/phase-2 (corrigida por R1 F1, ver AMENDED). Ratificacao e do orquestrador/gatekeeper, nao do autor (ground rule 2) | process-engine-architect (R1) |
| 2026-07-17 | DL-0029 | **CONTINGÊNCIA DL-0028 PARCIALMENTE LEVANTADA (extinção do SIP confirmada em fonte primária).** Texto integral da RN 639/2025 recuperado do endpoint legado da ANS (id 4696) e verificado por re-fetch independente byte-idêntico + control-norm probe (PR #62): Art. 1º revoga a RN 551/2022; Art. 2º desobriga o envio do SIP após o 4º trimestre/2025; Art. 3º vigência 02/03/2026 — obrigação extinta na data corrente. **Efeitos:** (a) o design de DECOMISSIONAMENTO do T2.6 (SP-OP-ANS-SUBMIT-001/ANS-CRON-001 automatizam obrigação extinta) está DESBLOQUEADO, incluindo a remoção mandatória do caminho de protocolo fabricado (ans_submit.py:209-212, rule 3) independente da direção; (b) o mecanismo SUCESSOR (Monitoramento TISS) está AUSENTE do texto primário — permanece gated em SME regulatório (somente avisos secundários); (c) hold do ans_calendar mantido até a direção do re-scope. | Requisito probatório da DL-0028 era confirmação da norma, não um humano específico; fonte primária obtida e verificada adversarialmente satisfaz a substância para (a); rule 3 impede assumir (b) sem fonte | orchestrator + adversarial-verifier (R1) |
| 2026-07-17 | DL-0028 | **T2.6 RE-SCOPE APROVADO (contingente):** SP-OP-ANS-SUBMIT-001/ANS-CRON-001 automatizam o envio SIP, extinto pela RN 639/2025 (revoga RN 551/2022; vigência 02/03/2026 → Monitoramento TISS). T2.6 muda de "corrigir citações RN" para "re-escopar os processos de envio ANS". **CONTINGÊNCIA ativa: confirmação do SME regulatório da RN 639/2025** — a verificação R1 do T2.5 só alcançou fontes secundárias (texto primário ANS inacessível no pass); nenhum re-scope executa antes dessa confirmação. | Achado T2.5 (PR #30, verified-as-draft R1, 9 fontes re-verificadas independentemente); decisão do dono 2026-07-17; rule 9 do plano exige amendment rastreável + notificação humana — este registro é ambos | Rodrigo + orchestrator |
| 2026-07-17 | DL-0027 | **PR #18 (hermes "M3 Core Runtime") FECHADO — SUPERSEDE-AND-CLOSE.** Runtime equivalente já mergeado em main como 89d91be (~3,5 min APÓS a abertura do PR — figura corrigida pelo verifier; não "3h antes"); 19/25 arquivos adicionados colidem por path com implementações divergentes já mergeadas; colisão Alembic revision="0001"; estado CONFLICTING/DIRTY. Fechado por timing + duplicação, não por qualidade (não é o arquétipo de fabricação). Ideia aproveitável (tiered-inference) preservada para T1.7 (design T1.1 §15). | Duas verificações R1 independentes (avaliação + re-medição das evidências); decisão do dono 2026-07-17 | Rodrigo + orchestrator |
| 2026-07-17 | DL-0026 | **ADR-0027 (transporte de auditoria Postgres-first, emenda à cláusula Kafka do ADR-0007) RATIFICADO → Accepted.** Nenhum cliente Kafka existe no src (B1); durabilidade de auditoria é necessária agora; critérios explícitos de re-entrada Kafka mantidos no ADR. | PR #41 verified (R1, 2 ciclos): kill-tests SIGKILL 3/3 sem perda, concorrência 2×100 sem fork, 8/8 corridas de genesis com vencedor único, canonicalização jsonb batida em 25/25 valores; decisão do dono 2026-07-17 | Rodrigo |
| 2026-07-03 | DL-0025 | **FECHAMENTO Wave-3 + programa 115/115 COMPLETO.** Reconcile final 22/22 (script exit 0: 20 merged + PAGTO-6 already-fixed + LGPD-reid blocked-by-design, ambos DL-0024); último gap GAP-NIP-6 = PR #171 (takeover T3 do snapshot preservado a3cf157, verifier T3 independente PASS em 7 gates com prova de mutação); **union-green assert: main @ 3cf828c com TODOS os 13 checks verdes incl. lane real-engine** (API check-runs pinada no SHA — regra pairwise≠union de DL-0023 satisfeita no commit final). Pós-programa: 4 briefs de residue novo em voo (#172 helm-orphan, #173 doc-juel-nested-text, #174 carolina-overclaim, pagto-a2a-envelope) + **res-resume-idempotency-durable DEFERRED para ADR** (exactly-once cross-restart exige decisão de infra redis-vs-postgres — não é micro-brief de swarm) + **res-phi-hmac-env-injection NOVO** (ExternalSecret cria maezo-phi-hmac mas nenhum deployment template injeta PHI_HMAC_KEY; provider fail-closed no startup em-cluster — achado do verifier do #172). | Regra do kickoff: wave fecha só com ledger script-reconciliado + union-green no commit de merge final; deferrals arquiteturais viram entrada rastreável em vez de build apressado | orchestrator-wave3 |
| 2026-07-03 | DL-0024 | Contabilidade de fechamento Wave-3 (ledger script-reconciliado): (1) **GAP-PAGTO-6 = ALREADY_FIXED** — a verificação C0 pós-Wave-2 provou que `clerical-pagamentos` entrou no allowlist de candidate-groups via a expansão do #144 (fix na camada de contrato/observabilidade; pagto_alcada.dmn segue emitindo o valor em r_dentro_teto_l2, agora enumerado) — nenhum build Wave-3 necessário. (2) **res-dispatcher-seam = ENTREGUE no PR #141** (branch fix/res-dispatcher-seam, main afeed7e, verifier T3 PASS + lane real-engine verde) — o corpo do PR cita os legs residuais GAP-CONTAS-1/GAP-PROG-1/GAP-LGPD-2; registrado aqui para visibilidade do token no reconcile. Sub-seam LGPD resolver titular_pseudo_id→fhir_patient_id permanece BLOCKED-by-design (sem store de reidentificação segura; fail-closed preservado). | Regra do kickoff: gap fechado OU explicitamente contabilizado com entrada rastreável; o script de reconcile lê decisions-log/review-queue como fonte de deferrals/credits | orchestrator-wave3 |
| 2026-07-03 | DL-0023 | HOTFIX direto no main (stop-the-line, precedente DL-0007): remoção da entrada manual `${grupo_humano}` de EXPRESSION_GROUP_RESOLUTIONS/EXPRESSION_DMN_SOURCE — trazida pelo #157 (registro bundled feito ANTES do #155 mergear) e proibida pelos testes-sanidade do #155 ("must stay resolved by the GENERAL flat-var mechanism"). Segunda instância da classe pairwise-green≠union-green NO MESMO ARQUIVO em uma sessão (#137×#144, depois #157×#155): CI de PR roda no merge-ref do momento do push e NÃO re-roda quando main avança antes do merge — lição operacional: re-validar lanes verificados-antigos contra main FRESCO antes de mergear quando main mudou no mesmo arquivo. Deleção provada segura localmente: 20/20 testes de candidate_groups verdes (o mecanismo geral do #155 resolve ${grupo_humano} identicamente — domínio nucleo-ans/regulatorio-ans/juridico-regulatorio). | Main vermelho bloqueia todos os merges; a deleção restaura o invariante do #155 sem perda funcional | orchestrator-wave3 |
| 2026-07-03 | DL-0022 | TAKEOVER Wave-2: o coordenador Wave-2 está INATIVO (diretiva do usuário); `orchestrator-wave3` assume TODA a conclusão Wave-2 + residue (#139 stdlib-scrub, lucas XHITL-4B/#140-fechado) — AMPLIA o escopo de DL-0021 (o `explicitly_not_mine` de fix/w2-*/#139/#140 fica SUPERSEDED; HANDOFF.yaml continua intocado até o close). Preservação executada ANTES de qualquer outra ação: snapshots commit+push de 8 worktrees sujos (arc c5c33ab, cancel 0971eb5 [10 arquivos, lane só-local], fc e959ff1, ia fbd1829, nip a8c6266, wapp 3b5364f, xobs56 4dfeb2e, logbridge b1c7dec). BOTTLENECK REGISTRADO: o classifier de auto-mode BLOQUEIA merge de PR autorado+verificado por agentes do próprio orquestrador (two-party review) — PRs verificados estacionam em READY-TO-MERGE (fila no HANDOFF-wave3.yaml) até merge humano ou regra de permissão; NENHUMA tentativa de contornar via push direto de branch (violaria a intenção do bloqueio). #141 é o primeiro estacionado (verifier PASS + integration pass). | Protocolo de preservação (memória Wave-1) manda snapshot antes de retomar lanes órfãs; único-orquestrador restaura os invariantes de DL-0015; fila explícita de merge mantém o gate humano audível | orchestrator-wave3 |
| 2026-07-03 | DL-0021 | Wave-3 (18 lows) + 4 itens de residue Wave-1 não-reivindicados (dispatcher-seam ×3 CONTAS-1/LGPD-2/PROG-1, NIP task_name label, sentinela competência ANS, paridade fail-safe de âncora) DELEGADOS a um SEGUNDO orquestrador paralelo (`orchestrator-wave3`), por diretiva do usuário — QUALIFICA DL-0016. Contrato de coordenação: propriedade de arquivos disjunta (fence em `docs/handoffs/HANDOFF-wave3.yaml`), hot files serializados first-merged-wins (nunca co-editados com fix/w2-* em voo), handoff SEPARADO (HANDOFF.yaml permanece exclusivo do coordenador Wave-2), gate de wave RESPEITADO (nenhum build Wave-3 antes de 41/41 gaps W2 reconciliados por script + main verde incl. lane real-engine; confirmado pelo usuário). O passo (6) "spawn Wave 3" do wave2_next_actions (#138) fica SUPERSEDED para esses itens; #139/#140/gauge user_task_pending permanecem do coordenador Wave-2. Residue Wave-1 NÃO é gateada — lançável quando file-disjunta dos diffs em voo (worker_runtime/service.py está em NENHUM diff aberto). | Dois orquestradores são legais só sob o contrato DL-0016; handoff separado evita contenção do HANDOFF.yaml (reescrito em #138); DL row + HANDOFF-wave3.yaml + comentário no PR #138 formam o fence anti-duplo-spawn com canal de leitura garantido | orchestrator-wave3 |
| 2026-07-02 | DL-0020 | **100% de cobertura BPMN DI obrigatória em todo `.bpmn`** — a convenção "DI adicionada na promoção a FINAL" está SUPERADA. Ferramenta de repo `src/maezo/platform/validation/bpmn_di.py` (gerador determinístico de auto-layout: ranking por caminho-mais-longo cycle-safe → grade centrada esquerda→direita → boundary events na borda do host, subProcess expandido com filhos dentro, arestas ortogonais) + subcomando CLI `python -m maezo.platform.validation.cli di [--check\|--write] [--force]` + regra de validação `check_di_coverage` (ERROR por flowNode sem BPMNShape / sequenceFlow sem BPMNEdge). É **preserva-se-completo**: a DI feita à mão de SP-OP-ESCALATION-001 (já 100%) é mantida byte-idêntica; só os 14 arquivos sem DI são regenerados. **Fase 1** (este PR): ferramenta + testes + regra, com o gate `bpmn.ENFORCE_DI_COVERAGE=False` (não regenera os 15 arquivos — outros PRs Wave-0 editam 3 deles). **Fase 2** (após merge dos PRs Wave-0): `di --write` em todos + trocar `ENFORCE_DI_COVERAGE=True`. | Diretriz vinculante do dono (2026-07-02): os `.bpmn` são abertos em viewers (Camunda Modeler) e a ausência de DI (1.073 elementos sem representação em 14 arquivos) impede revisão visual / human-in-the-loop. Auto-layout determinístico (mesma entrada → mesmos bytes) mantém o artefato versionável e a regeneração idempotente; a DI é ignorada pelo engine mas o output respeita a ordem XSD dos filhos (rootElements < BPMNDiagram; Bounds < BPMNLabel) para não quebrar o validador nem o deploy. Gate atrás de flag evita main vermelho enquanto os arquivos não foram regenerados (sequenciamento seguro, precedente DL-0014). | orchestrator/bpmn-di-coverage |
| 2026-06-14 | DL-0019 | ANS-SUBMIT retry/backoff modelado como subprocess embarcado `SUB_RetryEnvio` com DMN `ans_retry_policy` dirigindo contagem de tentativas + backoff exponencial (PT5M/PT30M/PT2H, DRAFT); worker `make_retransmit_handler` (topico `regulatorio.anssubmit.retransmit`) com o MESMO guard nao-repudio (`ERR_ANS_SUBMIT_NOT_HUMAN`) reusando a aprovacao humana — NUNCA re-autoriza; entrada via `ERR_ANS_PROTOCOLO_NACK` do submit; esgotamento via `ERR_ANS_RETRY_ESGOTADO` → `UT_TratarNack`. | ADR-0012 (DMN e a unica fonte de regra) — a politica de retry e regra ajustavel, logo vive em DMN, nao em literais de timer no BPMN; fecha a unica divergencia do audit `bpmn-process-completeness`; invariante HITL pre-filing preservada (guard no worker, nao no BPMN). | orchestrator/DOMAIN |
| 2026-06-14 | DL-0018 | Retenção de 5 anos do `audit_chain` (gap forense #6, ADR-0007) é NÃO-PARTICIONADA: tabela de 0002 mantida (`UNIQUE(prev_record_hash)` global, atômica) + `DELETE FROM audit_chain WHERE ts < cutoff` agendado (ISO-8601 lexicográfico) + retenção do tópico Kafka. Particionar foi REJEITADO. (run de auto-completion: 22 PRs #65–#86, todos os 37 gaps da auditoria forense resolvidos; relatório em `docs/reports/autonomous-completion-report.md`) | A lane real-engine PROVOU que particionar ENFRAQUECE a anti-fork (Seção 3 da auditoria): Postgres OBRIGA a UNIQUE de tabela particionada a incluir a chave de partição → `UNIQUE(prev_record_hash)` global degrada p/ `(prev_record_hash, ts)` + trigger cujo `IF EXISTS` NÃO é atômico sob READ COMMITTED → dois forks concorrentes ambos passam = fork silencioso (`test_concurrent_fork_*`: `assert 0==1`). 3 rounds confirmaram incompatibilidade: cast texto→timestamp/date é só STABLE (DateStyle), e coluna GENERATED não pode ser chave de partição. §7 (nunca enfraquecer guarda fail-closed) > escolha de implementação. Anti-fork volta à forma DB-atômica mais forte + retenção atendida | orchestrator |
| 2026-06-14 | DL-0017 | #55 conserta a lane de runtime (red main) com 2 correções + 1 exceção: (a) extensão pgvector criada em `public` (tipo/operador `vector` é DB-global, não por-tenant) — `0003 ... WITH SCHEMA public`, `env.py search_path "<tenant>", public`, `PostgresMemoryStore` search_path += public; (b) `PostgresAuditSink` usa asyncpg `setup=` (por-acquire), não `init=`; (c) EXCEÇÃO ADR-0011 forward-only: `0003_agent_memory` editada in-place | (a) sem isso o `register_vector` do asyncpg (search_path padrão) não acha o tipo, e o operador `<=>` (em public) fica invisível a um search_path só-do-tenant. (b) BUG REAL DE PRODUÇÃO achado por INSTRUMENTAÇÃO (3 teorias de migração estavam ERRADAS — a migração estava certa, audit_chain ERA criada): pools asyncpg rodam RESET ALL no release da conexão, zerando o `SET search_path` de sessão; com `init=` (só na criação), o 2º+ `emit()` numa conexão REUSADA pega o search_path padrão e o INSERT em audit_chain falha. (c) a 0003 nunca aplicou com sucesso em lugar algum (sempre vermelha, nada deployado), logo corrigir in-place não quebra deploy. Lição: instrumente bugs só-de-engine-real; use `setup=` p/ estado de sessão por-acquire | orchestrator/PLAT |
| 2026-06-13 | DL-0016 | Phase 3 BUILD DELEGADO a um SEGUNDO orquestrador PARALELO (QUALIFICA DL-0015) — brief em `docs/handoffs/phase3-orchestrator-brief.md`, estado em `HANDOFF-phase3.yaml` | Mudança de fator: pressão de janela de contexto do orquestrador de runtime (>85%). Offload de uma track grande p/ um contexto FRESCO reduz o risco de uma compaction disruptiva e deixa runtime (W-R2/W-R3/Track-S code) e domínio (Phase 3) progredirem em paralelo sem consumir o contexto escasso do runtime. As objeções de DL-0015 são endereçadas por um CONTRATO DE COORDENAÇÃO: partição de propriedade de arquivos; arquivos COMPARTILHADOS (a2a, process_allowlist, topic_registry, autonomy) coordenados e NÃO co-editados; dependência A2A resolvida por SEQUENCIAMENTO (W-D agentes esperam W-R2/R9 mergear; W0/W-A/W-B/W-C não precisam de runtime); handoff SEPARADO (HANDOFF-phase3.yaml) evita contenção do single-HANDOFF; worktrees isolados (files/gates/caches). Runtime-first (DL-0014) preservado: Phase 3 build não bloqueia nem é bloqueado exceto no seam A2A | orchestrator |
| 2026-06-13 | DL-0015 | Phase 3 NÃO delegado a um orquestrador PARALELO; tratado por ESTE orquestrador, sequenciado após Track R, com overlap de fundações disjuntas em worktrees | Dois orquestradores paralelos quebram os invariantes de orquestrador-único (1 HANDOFF/fonte-da-verdade, 1 autoridade dono-de-arquivo, 1 fila de merge/stop-the-line) e não têm protocolo de coordenação; colisões reais de arquivo (a2a/dispatcher: R9 ∩ delegações dos agentes Phase 3; process_allowlist.py; config/topic_registry.yaml; audit.py↔custody.py) e o A2A é dependência DURA — agentes Phase 3 constroem sobre a assembly de produção de W-R2/R9 (runtime-first, DL-0014). Paralelismo SEGURO = worktrees isolados (files/gates/caches/branch separados) sob UM orquestrador: agora W-R1 (PR #48) + Track C (worktree) + Phase 3 foundations (worktree) em paralelo | orchestrator |
| 2026-06-13 | DL-0014 | Plano de plataforma entregue (`docs/reports/platform-completion-plan.md`); sequenciamento RUNTIME-FIRST: Track R (runtime-wiring) antes de mais Phase 3 | Uma sweep forense de 6 domínios (read-only, cross-checada na working tree @b2641f5) achou a tese central: a plataforma é **biblioteca-com-testes, não roda como serviço** — 0 `__main__` em src, sem Dockerfile de app, 4 Deployments Helm apontam p/ módulos sem `__main__`/inexistentes (crash-loop), harness sem health server, audit hash-chain + idempotência A2A em memória (forka/duplica no restart). Autorar mais Phase 3 sobre um runtime que não executa os 9 processos + 5 agentes atuais só acumula BPMN nunca-executado. Logo o trabalho de maior alavancagem é o runtime-wiring (R1+R2+R3+R5), 100% buildable sem AWS. ~57 gaps consolidados (de ~107 raw) em 5 tracks (R/C/3/S/P) | orchestrator |
| 2026-06-13 | DL-0013 | ADR-0018 codifica o padrão estrutural no-denial de 5 partes como VINCULANTE para todo SP-OP negativa-like | Operacionaliza ADR-0005/0008/0012 numa invariante por construção (tipos sem variante adversa + DMN sem saída de negativa + fail-safe a User Task por allowlist fechada + worker guard ERR_*_NOT_HUMAN + teste de integração na história do engine real). Provado + CI-enforced em AUTH-001/CONTAS-001; replicado nos 5 bodies de PR #38; mandato p/ futuros (SP-OP-FRAUDE, SP-OP-INADIMPLENCIA) | ARCH/DOCS |
| 2026-06-13 | DL-0012 | ADR-0017 operacionaliza a metade de REDE da ADR-0006 (egress PHI por CIDR IP-fixado, fail-closed) | Uma revisão adversarial pegou um egress `0.0.0.0/0` que tornava a NetworkPolicy PHI um no-op (permitia a internet inteira incl. LLM cloud geral). Chart `maezo-tenant` fail-close (helm `fail`) em CIDR PHI ausente/`0.0.0.0/0`; intra-namespace escopado ao gateway (quebra relay PHI→geral→cloud); securityZone cross-validado contra a definição mergeada. FQDN exige egress proxy/Cilium toFQDNs (FUTURO, não entregue) | ARCH/DOCS |
| 2026-06-13 | DL-0011 | `src/maezo/platform/validation/bpmn.py` excluido do `ruff format` (não do `ruff check`) via `[tool.ruff.format].exclude` | Divergencia cross-platform/não-determinística do `ruff format` 0.15.17 (pinado): o runner CI manylinux intermitentemente quer reformatar um literal de coleção que o macOS arm64 (e outras execuções CI, ex. main @1c63714 verde) consideram já formatado — mesmos bytes, mesma versão, veredito diferente. Bloqueava `make lint` de TODO PR (gate tree-wide). Stop-the-line (§4, precedente DL-0007); lint (E/F/I/N/W/UP/B/SIM/ASYNC) continua cobrindo o arquivo. Revisitar quando o ruff resolver | orchestrator |
| 2026-06-13 | DL-0010 | ADR-0016 adicionado (process_key allowlist + invariante PHI no ToolRegistry) | Decisão de arquitetura de segurança com enforcement estrutural; justifica ADR proprio (não apenas DL): cria nova camada de recusa entre PEP e handler, e torna ADR-0006 uma invariante verificável em código (PR #19) | ARCH/DOCS |
| 2026-06-13 | DL-0009 | ADR-0015 formaliza runtime A2A de PR #17 (complementa ADR-0003) | ADR-0003 escolheu protocolo; PR #17 tomou decisoes concretas de estrutura de dados, contrato PHI no envelope e injecao de handlers que precisam de ADR proprio | ARCH/DOCS |
| 2026-06-13 | DL-0008 | ADR-0014 converte recomendacao W7 (DL-W7-001) em decisao aceita: AMP/AMG para staging/prod; Prometheus/Grafana in-cluster para dev | Avaliacao de custo W7 (~$30–50/mês Phase 0-1 < limiar §2-bis); alinhamento com modulo observability-stack da amh-data-platform; PR #21 ja implementou | ARCH/DOCS |
| 2026-06-12 | DL-0007 | Hotfix direto no main: regex de filename BPMN aceita underscore no Titulo | Main vermelho (validator W1 × convenção usada pelo W5 nos 3 BPMN mergeados); stop-the-line §4 | orchestrator |
| 2026-06-12 | DL-0006 | Imagem CIB Seven pinada em 2.1.0 | Tag 2.1.3 dos ADRs não existe no Docker Hub; 2.1.0 é a mais próxima na linha 2.1.x; revisitar quando publicarem | orchestrator |
| 2026-06-12 | DL-0005 | PEP: L0 `hard` → DENY; L0 não-hard → REQUIRE_HUMAN (dossiê); L1 → REQUIRE_HUMAN; L2/L3 → ALLOW | Mapeamento ADR-0005/0008 proposto pelo W2 e aprovado em review ARCH (PR #1) | orchestrator |
| 2026-06-12 | DL-0004 | PR #2 (validation CLI) mergeado com jobs integration/evals vermelhos | Falhas pré-existentes de infra (imagem `cibseven/cibseven:2.1.3` inexistente; job evals sem harness) já no escopo do W7; quality+artifact-validation verdes | orchestrator |
| 2026-06-12 | DL-0003 | Updates de HANDOFF.yaml/decisions-log.md vão por push direto do orquestrador | Metadados de estado do swarm, não código; PR para cada update geraria churn sem revisor | orchestrator |
| 2026-06-12 | DL-0002 | Sem infra CDC neste repo: consumimos os tópicos Debezium da amh-data-platform (CDC Tasy já em produção lá: Debezium/ECS Fargate + MSK Serverless) | §2-bis (precedência da Plataforma de Dados); ADR-0013 formaliza (W3) | orchestrator |
| 2026-06-12 | DL-0001 | Log inicializado no kickoff do swarm Phase 0 | §9 do prompt de execução | orchestrator |
| 2026-09-08 | DL-0048 | Portal humano próprio e comandos atômicos — ADR-0049; substitui a escolha histórica Tasklist-only, preservada como histórico | Decisão de produto/engenharia autorizada pelo plano do usuário: primeiro plano `/Users/familia/Downloads/plan.md:78`, SHA-256 d0218f1dff955bd09fe84300121c92412d91c0b739fdc847c71c50f9794e71c7; consolidação `docs/plan.md` §3 Wave 1, §4–§5. ID de coordenação novo PLAN-PRODUCT-PORTAL-SUPERSEDE-TASKLIST, sem inventar ID anterior ou assinatura. ADR define 43 tarefas/15 famílias, gateway humano separado, principal/workload, comando assinado + receipt na TX do engine, audit intent/outbox na TX tenant e reconciliação; emenda delimitada dos pressupostos K8s/EKS para ECS/Fargate sa-east-1 com isolamento/PHI preservados. Dúvida PAGTO de vocabulário exige reconciliação posterior do catálogo/contrato; lastro não confirma obrigação. Esta entrega é docs-only DRAFT/verify da especificação: implementação, revisão independente, ratificações clínicas/DPO e release exato continuam pendentes. Nenhum ADR Accepted antigo alterado | Plano do usuário (produto/engenharia); redação AGENTE Astra; verificação independente pendente |
