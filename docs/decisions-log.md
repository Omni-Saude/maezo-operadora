# Decision Log — Maezo Healthcare Plan

Decisões não-triviais do orquestrador/swarm que não justificam ADR próprio.
Decisões arquiteturais relevantes continuam virando ADR (`docs/adr/`).

Formato: `| data | id | decisão | racional | quem |` — uma linha por decisão, mais recente no topo.

| Data | ID | Decisão | Racional | Quem |
|---|---|---|---|---|
| 2026-07-25 | DL-0035 | **ADR-0033 PROPOSTO (identidade de servico do agente — emissao/verificacao do certificado do T-G) — pendente ratificacao do arquiteto de seguranca/infra.** Documento produzido para ACELERAR a decisao humana, nao para substitui-la: estrutura o espaco de decisao (drivers, 3 opcoes com trade-offs, perguntas abertas) para a metade certificado/service-account de T-G que ADR-0007:10 pede numa unica clausula sem elaborar mecanismo de emissao/verificacao — DESIGN-GAP ja sinalizado por ADR-0032:67-73 e por `docs/design/A2A-dispatcher-card-signing.md` §4/§7.1 ("This needs a NEW ADR before any cert-issuance code is written"). Opcoes cobertas: (A) SPIFFE/SPIRE workload identity + mTLS; (B) Vault-issued short-lived service-account JWT por agente+tenant+agent_version (RECOMENDADA nao-vinculante); (C) certificado estatico por agente de CA interna. Recomendacao funda-se em infra REAL verificada nesta sessao: `deploy/terraform/modules/` nao tem `kms`/`acm`/`pca` hoje; o unico seam de "vault" existente e ESO+AWS Secrets Manager autenticado por IRSA (`externalsecret.yaml:2,11`); ServiceAccount ja e per-agente via IRSA (`deployment-agent-runtime.yaml:8`) mas e identidade de nuvem, nao de aplicacao; nenhum service mesh (Istio/Linkerd/SPIFFE/SPIRE) esta implantado. Caminho de migracao interim->cert-backed documentado (AUDIT_AGENT_ID="operadora-worker", `harness.py:111`, continua funcionando sem mudanca ate ratificacao+provisionamento externo). NAO EDITA ADR-0007 — **amends** (elabora a clausula `:10`), nao supersede, seguindo o precedente ADR-0027/ADR-0032 (`docs/adr/README.md:8`). Complementa (nao bloqueia nem e bloqueada por) a metade Agent-Card-assinado de T-G ja construida em `t2.4-a2a-w1-card-signing` (branch separada, nao mergeada) nem T-F (delegacao A2A, roda sobre `emit_once`, W4) — ambas ORTOGONAIS a este ADR. BUILD real da metade cert fica bloqueado ate (a) ratificacao humana e (b) vault/KMS/CA provisionado (dependencia externa). Sessao DOCS-ONLY: nenhuma edicao de `src/`/`tests/`/`spec/`/evidence-ledger; esta e arquitetura NAO-TASKEADA (nao fecha nenhum `T<fase>.<n>` do backlog), por isso nao carrega trailer `Task:` no commit nem linha no evidence-ledger. | Brief orquestrador (T-G e a unica peca A2A/audit-identity genuinamente ADR-gated per ADR-0032 + a recon A2A); fundado em leitura direta de ADR-0007/0032, `A2A-dispatcher-card-signing.md`, `src/maezo/a2a/{card,signing,registry,assembly}.py` (branch W1), `AUDIT_AGENT_ID`/`AgentDecisionProvenance` (grep+leitura), `docs/adr/README.md` (convencao) e a infra real (`deploy/terraform/modules/`, `deploy/helm/maezo-tenant/templates/`). Ratificacao e do arquiteto de seguranca/infra + gatekeeper, nao desta sessao. | docs-adr-agent (T-G identity ADR) |
| 2026-07-24 | DL-0034 | **PROPOSTO (T2.9 SME-packaging pass) — pendente ratificacao do orquestrador.** Workers de notificacao da familia escalation (`operadora.escalation.notify_team`/`notify_supervisor`/`notify_fallback`, `SP-OP-ESCALATION-001`) devem publicar worker-side via RAW ASYNC KAFKA HANDLERS (mesma forma sancionada do #55 R-B `operadora.lgpd.request_additional_proof`/`events.py`) quando forem religados — isto NAO e uma excecao a "pureza dict-first" do ADR-0026 §2, e a forma CORRETA para esta classe de tarefa: para escalation, notificar E o proprio efeito de negocio (nao ha calculo de dominio algum alem de rotear+notificar), e o processo nao modela estes 3 external tasks como o `operadora.events.publish` generico — sao topicos PROPRIOS. Evidencia: `NotifyTeamWorker`/`NotifySupervisorWorker` sao `WorkerBase` (sync, dict-first) e `register_escalation_workers` faz `del kafka, seams  # unused` (`escalation.py:199`) — por design ADR-0026 §2 uma classe `WorkerBase` nao tem seam Kafka async utilizavel dentro de `execute()`, entao a notificacao NUNCA acontece de fato (so um dict `{"status": "teams_notified", ...}` e retornado). `tests/integration/processes/test_sp_op_escalation_001.py:91-115` (`_NOTIFY_KAFKA_GAP_REASON`) documenta isto como gap confirmado AO VIVO (CIB Seven 2.1.0): `probe.notified_teams`/`notified_supervisors` NUNCA observam uma execucao e `ERR_ESC_NOTIFY_FAILED`/o boundary de fallback NUNCA disparam (nada chama `kafka.publish` para estes tipos). A tabela de 24 boundary catches do ADR-0030 ja classifica `ERR_ESC_NOTIFY_FAILED` como G2-fs (fail-safe tecnico) com "Worker raises it today? NO (Kafka/notify gap)" — o MESMO defeito, ja agendado para o Tier 1 da migracao ADR-0030 ("entangled with the Kafka-producer gap ... co-schedule"). Precedente ja sancionado e ao vivo: #55 R-B (ADR-0031 decisao 4) e um raw handler async registrado via `harness.register(topic, handler)` (nao `WorkerBase`) exatamente porque seu unico efeito e notificar, publicando no MESMO topico interno `operadora.notifications.internal` que escalation usaria (`lgpd.py:475` vs `escalation.py:84`). Este DL fixa a FORMA (raw handler) a adotar quando a implementacao real acontecer (tarefa de build futura, fora do escopo desta sessao doc-only); nao autoriza nem agenda o build em si. **AMENDED 2026-07-25 (pos-merge `origin/main` 6288341):** (a) o topico morto `operadora.escalation.notify_fallback` e seu `NotifyFallbackWorker` foram DELETADOS pelo #133 (t2.5-p2b-round2 — zero `camunda:topic` correspondente em `spec/`, ver `escalation.py:152-157`); este DL passa a cobrir APENAS `notify_team`/`notify_supervisor`. (b) Citacoes re-pinadas: `del kafka, seams` agora em `escalation.py:168`; `_NOTIFY_KAFKA_GAP_REASON` em `test_sp_op_escalation_001.py:107-119` (xfails em `:399/:469/:513`); a citacao original '`escalation.py:84`' referia-se na verdade ao arquivo de TESTE (`test_sp_op_escalation_001.py:84`, `_NOTIFICATIONS_TOPIC`), e `lgpd.py:475` agora e `lgpd.py:405`. (c) Substancia INALTERADA e re-verificada pos-merge: os dois notify workers seguem `WorkerBase` sem nenhum `kafka.publish` (grep 2026-07-25). | Sessao de packaging SME (T2.9, `docs-p2b-escalation-packages`) — achado ao auditar #55 R-B/R-F/R-G para os pacotes DPO/médico-auditor; fundado em escalation.py/ADR-0026/ADR-0030/ADR-0031 + o teste de integracao ao vivo. Ratificacao e do orquestrador, nao desta sessao. | sme-packaging-agent (T2.9) |
| 2026-07-24 | DL-0033 | **PROPOSTO (T2.9 SME-packaging pass) — pendente ratificacao do orquestrador.** Os workers de dossie A2A Carolina/Andre (`operadora.cred.prepare_dossier`, `operadora.pagto.prepare_approval_dossier`, `operadora.adequacao.prepare_remediation_dossier`) devem ser construidos como STUBS LOCAIS (`FunctionWorker`, echo/log informativo, SEM `DelegationDispatcher`), espelhando o precedente `programa.enroll_beneficiario` (L3, neutro, "enrollment is not an adverse effect" — `programa.py:77-95`) — a INTEGRACAO A2A REAL (`DelegationEnvelope` + `DelegationDispatcher.delegate` ate Carolina/Andre) fica DEFERIDA a uma tarefa futura de wiring-A2A completo (rotulada "M5" no brief orquestrador; nota de drift: o repo ja tem um marco historico chamado M5 — "feat(M5): Agent Framework + A2A + ..." per `docs/reports/T0.3-agent-yaml-drift.md:16` — este DL NAO reabre aquele marco passado, apenas usa o mesmo rotulo do brief para uma tarefa de fechamento futura; a orquestracao deve confirmar/renomear o rotulo ao agendar). Racional: hoje os tres topicos sao GAPS confirmados sem NENHUMA funcao implementadora, nao apenas "delegacao parcial" — `credenciamento.py:296-297`, `pagto.py:376-377`, `adequacao.py:266-267` listam os tres, respectivamente, no comentario "Spec topics with NO implementing function today (gap, not fabricated here)"; `andre/graph.py:12,30` e o `agent.yaml`/`accepted_task_types` de Carolina ja declaram os `task_type` recebedores (`pagto_dossier`/`adequacao_dossier`; `credentialing.analyze`) mas NENHUM call site origina o envelope (grep confirmado); `docs/reports/predeploy-findings.json` ja cataloga cada um destes como achado disclosed (GAP prepare_dossier de CRED sem `DelegationDispatcher`; RECURSO sem parametro `dispatcher`; INADIMPLENCIA→Fernando faltando o kwarg `dispatcher=` na wiring de producao). Construir um stub local AGORA fecha o orfanato de topico BPMN (as User Tasks humanas hoje abrem SEM dossie algum) sem inventar logica de negocio-de-delegacao antes do dispatcher real estar pronto — mesmo raciocinio que motivou o precedente `enroll_beneficiario`. NAO ratificado — apenas recomendacao desta sessao de packaging; requer aprovacao do orquestrador antes de qualquer build. **AMENDED 2026-07-25 (pos-merge `origin/main` 6288341):** os tres topicos SEGUEM gaps mesmo apos #130 (cred family reconciliation) e #133 (pagto/adequacao round-2) — comentarios de gap re-pinados: `credenciamento.py:437-438` ('Carolina A2A integration, separately gated'), `pagto.py:550-551` e `adequacao.py:348-349` ('Andre A2A-gated — out of scope for t2.5-p2b-round2'); os proprios builders da round-2 deferiram exatamente estes tres, corroborando a direcao deste DL. `enroll_beneficiario` permanece em `programa.py:77-95` (citacao inalterada). | Sessao de packaging SME (T2.9, `docs-p2b-escalation-packages`) — achado ao auditar o estado real dos tres workers de dossie contra `docs/review-queue.md` GAP-ADEQ-4 (cujo texto descreve `prepare_remediation_dossier` como ja delegando a Andre via `DelegationDispatcher` — DIVERGE do codigo atual de `adequacao.py`, que ainda lista o topico como gap; flagged como drift de documentacao, nao corrigido aqui pois `review-queue.md` foi editado apenas com um append minimo por esta mesma sessao, nao uma reconciliacao retroativa). Ratificacao e do orquestrador, nao desta sessao. | sme-packaging-agent (T2.9) |
| 2026-07-24 | DL-0032 | **ADR-0032 RATIFICADO (orquestrador, 2026-07-24) — corrige a alegação de "runtime completo" do ADR-0015 (é do donor v1 PR #17, não do v2).** ADR-0015 (Accepted 2026-06-13) afirma em `:14` "A PR #17 implementou o runtime completo em `src/maezo/a2a/`" e descreve `dispatcher.py`/`delegation.py`/`facts.py` como se existissem em v2. Reverificado 2026-07-24 contra `main=1318e1b`: `src/maezo/a2a/` tem só 4 arquivos (`__init__.py`/`anti_loop.py`/`card.py`/`registry.py`, único commit tocante `7e76006`); `git log --all --diff-filter=A` para `dispatcher.py`/`facts.py`/`delegation.py`/`idempotency.py` retorna ZERO em todo o histórico — nunca existiram neste repo; `AgentCard`/`A2ARegistry`/`AntiLoopGuard` têm ZERO call site de produção (só docstrings de gap em `rafael/graph.py:48-50`, `beatriz/graph.py:89-90`, `marina/graph.py:115`); `PLANS.md:43` já rastreia "A2A dispatcher + card-signing NÃO feitos (backlog P3)"; DL-0009 (contemporâneo à autoria do ADR) já registrava "ADR-0015 formaliza runtime A2A de PR #17" sem distinguir donor/v2. **Amends (não supersede) ADR-0015**, per `docs/adr/README.md:8` + precedente ADR-0027/DL-0026 (nova ADR corrige, arquivo original intocado — histórico é histórico). Consequências: T-F (auditoria de delegação A2A, `docs/design/audit-emit-path-wiring.md:387-388,627-628`) e a metade signed-Agent-Card de T-G (`:389-391,629`) ficam BLOCKED-ON este substrato — sem dispatcher não há chokepoint pra instrumentar ou verificar; a metade service-account/cert de T-G é um DESIGN-GAP próprio (ADR-0007 só tem uma cláusula em `:10`, sem mecanismo de emissão nem chokepoint de verificação) que precisa de ADR dedicado, fora do escopo desta emenda. Aponta para `docs/reports/T2.4-a2a-agent-card-signing-gap.md` (2026-07-17), que chegou à mesma conclusão pro signing. ZERO edição de `src/`/`tests/`/`spec/`/evidence-ledger; `make validate-artifacts`/`make validate-signoff` verdes. | Brief orquestrador (correção de status ADR-0015 ground-truth); reverificação direta da árvore nesta sessão (git log/grep + leitura de PLANS.md/ADR-0007/ADR-0027/DL-0009/T2.4-report) — nenhuma citação aceita sem re-pin. Ratificação é do orquestrador/gatekeeper, não do autor (ground rule 2) | docs-agent (R2, correção ADR-0015) |
| 2026-07-19 | DL-0031 | **ADR-0031 PROPOSTO (LGPD DSR identity gate fail-closed) — pendente ratificacao DA POLITICA DE PRODUTOR pelo DPO.** Decisao: `ValidateIdentityWorker` confirma identidade SO com sinal explicito `identidade_verificada is True` (ausente/False/lixo -> NAO confirmada -> challenge sub-flow); a mera presenca do `titular_pseudo_id` (OBRIGATORIO) NUNCA confirma (fecha o defeito fail-OPEN A3). GAP-LGPD-6: `titular_pseudo_id` vazio -> **RAISE** `WorkerBpmnError(ERR_DSR_IDENTITY_UNVERIFIED)` (antes retornava o codigo como VALOR de dict, deixando `BE_IdentidadeInverificavel`/`End_IdentidadeInverificavel` inalcancaveis) — guard TECNICO, NUNCA acusacao de fraude (L0 `fraud_accusation` intacto); `max_retries=1` (guard deterministico, espelha auth). `ERR_DSR_IDENTITY_UNVERIFIED` e consumption-covered + NAO-adverso -> Tier-0 (unido em `service.py` via `LGPD_BPMN_ERROR_ALLOWLIST`, NAO T-E-gated). Construido o worker #55 R-B `request_additional_proof` (raw handler async; SO pede prova, NUNCA seta `identidade_verificada`) para o challenge nao travar em prod. Dois produtores ratificados do sinal (seed de canal autenticado DPO-ratificado; correlacao humana `msg.lgpd.proof_received` que reverifica) — **so a POLITICA DE PRODUTOR** (quais canais/quais provas) fica pendente do DPO; a semantica CONSUMIDORA (fail-closed) nao espera por isso. ZERO edicao de BPMN/DMN (o modelo ja tinha boundary + loop de reverificacao). 4 strict-xfails T3.1 flipados (boundary/expira/pede-prova/prova-recebida), cada um provando a propriedade fail-closed em engine real. Fundado em LGPD Art. 18/11/19 II + o threat model anti eng.-social do proprio processo (`bpmn:87`, `contract:11`), conformante com ADR-0030. | Brief orquestrador-ratificado (design a6722411, Opcao C); fundado na arvore (`main=a86195e`) apos reverificacao de lgpd.py/service.py/harness/base + boundary-proof gate + BPMN. Ratificacao e do orquestrador/gatekeeper; a politica de produtor e do DPO (rule: nao inventar politica de identidade sem sign-off). | lgpd-identity-impl-engineer (T2.8) |
| 2026-07-18 | DL-0030 | **ADR-0030 PROPOSTO (semantica de erro de worker vs boundary catches do BPMN) — pendente ratificacao.** Decisao: **Opcao A — `WorkerBpmnError` modelado**, com a linha modelado-vs-incidente tracada EXATAMENTE na declaracao do boundary catch no spec: um worker levanta `WorkerBpmnError(code)` sse-e-somente-se `code` for um `bpmn:error@errorCode` declarado num error boundary event anexado ao external task daquele worker com COBERTURA DE CONSUMO — declarado em todo processo consumidor, OU restrito aos processos declarantes por condicao de dispatch worker-side verificada mecanicamente contra o spec (caso `operadora.events.publish`/`ERR_EVENT_PUBLISH_FAILED`: filtro events.py:120-127, gate :259 — F2) — provado pelo boundary-proof gate (clausula (c) faseada warn→hard durante a migracao, F5); todo o resto (falhas inesperadas, input mau/imutavel, guards L0 SEM boundary modelado) permanece incidente-fail-closed. Correcao do brief fundada na arvore: o mecanismo `WorkerBpmnError`+allowlist JA EXISTE (harness.py:129,916-943) e auth/cancel/events ja o levantam; o bloqueador sistemico real e a allowlist de producao VAZIA (`service.py:300 frozenset()`) + o boundary-proof gate NAO-construido, nao a reclassificacao para `ValueError` (que so afeta excecoes de dominio codificadas, base.py:284-294). **24 boundary catches em external tasks enumerados** em 12 familias (auth/ans/cancel/cred×3/escalation×8/inadimplencia/lgpd/nip×3/pagto/programa/recurso×2/reembolso; adequacao/contas/fraude tem ZERO). Refina ADR-0026 §5 (gate promovido de follow-up a pre-condicao); agenda T-E (audited-refusal) como co-requisito duro para TODOS os codigos `*_NOT_HUMAN` + o denial-block — incl. `ERR_CANCEL_MANTER_NOT_HUMAN` do G1 (F4: ativar um guard pre-T-E trocaria incidente humano-visivel por fim limpo silencioso) — sem contradizer o deferral atual do audit-emit design. Opcao B (podar os catches) REJEITADA (deletaria 24 caminhos de seguranca nao-adversos modelados; ADR-0029 e poda de audit_chain, nao de BPMN). DOCS-ONLY; nenhuma edicao de src/spec/test. **AMENDED 2026-07-18 por verificacao adversarial R1 round 1 (F1-F6):** evidencia de xfail CORRIGIDA — os 19 strict-xfails das 3 suites **phase-1** sao o Kafka-producer gap (sub-claim exata), mas as 13 suites **phase-2** (`t3.1-process-suites-phase2-land` @ d27264e) carregam **≥11 (13 incl. o par composto cred guard-shape `_CRED_GUARD_SHAPE_MISMATCH_REASON` ×2, cujos testes o autor rotulou pelo bloqueador PRIMARIO de worker faltante — mesma classe por mecanismo secundario identico, correcao de contagem por reverificacao R1) strict-xfails documentando EXATAMENTE esta classe** (programa×2/nip×3/cred×1/lgpd×1/recurso×2/ans_submit×2/cred-guard-shape×2) — 11 FLIPAM nos Tiers 1-2, o par composto no Tier 3 (xfail→XPASS→marker removido com prova live) — evidencia que FORTALECE a Opcao A; pares (topic,errorCode) distintos corrigidos 15→17 (15 = errorCodes distintos); `events.py:193 ERR_PUBLISH_MISSING_TOPIC` catalogado (cleanup Tier-0). | Brief T3.1 (process-engine-architect R1); fundado na realidade do repo apos verificacao de harness/base/service + enumeracao BPMN + evidencia de xfail phase-1/phase-2 (corrigida por R1 F1, ver AMENDED). Ratificacao e do orquestrador/gatekeeper, nao do autor (ground rule 2) | process-engine-architect (R1) |
| 2026-07-17 | DL-0029 | **CONTINGÊNCIA DL-0028 PARCIALMENTE LEVANTADA (extinção do SIP confirmada em fonte primária).** Texto integral da RN 639/2025 recuperado do endpoint legado da ANS (id 4696) e verificado por re-fetch independente byte-idêntico + control-norm probe (PR #62): Art. 1º revoga a RN 551/2022; Art. 2º desobriga o envio do SIP após o 4º trimestre/2025; Art. 3º vigência 02/03/2026 — obrigação extinta na data corrente. **Efeitos:** (a) o design de DECOMISSIONAMENTO do T2.6 (SP-OP-ANS-SUBMIT-001/ANS-CRON-001 automatizam obrigação extinta) está DESBLOQUEADO, incluindo a remoção mandatória do caminho de protocolo fabricado (ans_submit.py:209-212, rule 3) independente da direção; (b) o mecanismo SUCESSOR (Monitoramento TISS) está AUSENTE do texto primário — permanece gated em SME regulatório (somente avisos secundários); (c) hold do ans_calendar mantido até a direção do re-scope. | Requisito probatório da DL-0028 era confirmação da norma, não um humano específico; fonte primária obtida e verificada adversarialmente satisfaz a substância para (a); rule 3 impede assumir (b) sem fonte | orchestrator + adversarial-verifier (R1) |
| 2026-07-17 | DL-0028 | **T2.6 RE-SCOPE APROVADO (contingente):** SP-OP-ANS-SUBMIT-001/ANS-CRON-001 automatizam o envio SIP, extinto pela RN 639/2025 (revoga RN 551/2022; vigência 02/03/2026 → Monitoramento TISS). T2.6 muda de "corrigir citações RN" para "re-escopar os processos de envio ANS". **CONTINGÊNCIA ativa: confirmação do SME regulatório da RN 639/2025** — a verificação R1 do T2.5 só alcançou fontes secundárias (texto primário ANS inacessível no pass); nenhum re-scope executa antes dessa confirmação. | Achado T2.5 (PR #30, verified-as-draft R1, 9 fontes re-verificadas independentemente); decisão do dono 2026-07-17; rule 9 do plano exige amendment rastreável + notificação humana — este registro é ambos | Rodrigo + orchestrator |
| 2026-07-17 | DL-0027 | **PR #18 (hermes "M3 Core Runtime") FECHADO — SUPERSEDE-AND-CLOSE.** Runtime equivalente já mergeado em main como 89d91be (~3,5 min APÓS a abertura do PR — figura corrigida pelo verifier; não "3h antes"); 19/25 arquivos adicionados colidem por path com implementações divergentes já mergeadas; colisão Alembic revision="0001"; estado CONFLICTING/DIRTY. Fechado por timing + duplicação, não por qualidade (não é o arquétipo de fabricação). Ideia aproveitável (tiered-inference) preservada para T1.7 (design T1.1 §15). | Duas verificações R1 independentes (avaliação + re-medição das evidências); decisão do dono 2026-07-17 | Rodrigo + orchestrator |
| 2026-07-17 | DL-0026 | **ADR-0027 (transporte de auditoria Postgres-first, emenda à cláusula Kafka do ADR-0007) RATIFICADO → Accepted.** Nenhum cliente Kafka existe no src (B1); durabilidade de auditoria é necessária agora; critérios explícitos de re-entrada Kafka mantidos no ADR. | PR #41 verified (R1, 2 ciclos): kill-tests SIGKILL 3/3 sem perda, concorrência 2×100 sem fork, 8/8 corridas de genesis com vencedor único, canonicalização jsonb batida em 25/25 valores; decisão do dono 2026-07-17 | Rodrigo |
| 2026-07-03 | DL-0025 | **FECHAMENTO Wave-3 + programa 115/115 COMPLETO.** Reconcile final 22/22 (script exit 0: 20 merged + PAGTO-6 already-fixed + LGPD-reid blocked-by-design, ambos DL-0024); último gap GAP-NIP-6 = PR #171 (takeover T3 do snapshot preservado a3cf157, verifier T3 independente PASS em 7 gates com prova de mutação); **union-green assert: main @ 3cf828c com TODOS os 13 checks verdes incl. lane real-engine** (API check-runs pinada no SHA — regra pairwise≠union de DL-0023 satisfeita no commit final). Pós-programa: 4 briefs de residue novo em voo (#172 helm-orphan, #173 doc-juel-nested-text, #174 carolina-overclaim, pagto-a2a-envelope) + **res-resume-idempotency-durable DEFERRED para ADR** (exactly-once cross-restart exige decisão de infra redis-vs-postgres — não é micro-brief de swarm) + **res-phi-hmac-env-injection NOVO** (ExternalSecret cria maezo-phi-hmac mas nenhum deployment template injeta PHI_HMAC_KEY; provider fail-closed no startup em-cluster — achado do verifier do #172). | Regra do kickoff: wave fecha só com ledger script-reconciliado + union-green no commit de merge final; deferrals arquiteturais viram entrada rastreável em vez de build apressado | orchestrator-wave3 |
| 2026-07-03 | DL-0024 | Contabilidade de fechamento Wave-3 (ledger script-reconciliado): (1) **GAP-PAGTO-6 = ALREADY_FIXED** — a verificação C0 pós-Wave-2 provou que `clerical-pagamentos` entrou no allowlist de candidate-groups via a expansão do #144 (fix na camada de contrato/observabilidade; pagto_alcada.dmn segue emitindo o valor em r_dentro_teto_l2, agora enumerado) — nenhum build Wave-3 necessário. (2) **res-dispatcher-seam = ENTREGUE no PR #141** (branch fix/res-dispatcher-seam, main afeed7e, verifier T3 PASS + lane real-engine verde) — o corpo do PR cita os legs residuais GAP-CONTAS-1/GAP-PROG-1/GAP-LGPD-2; registrado aqui para visibilidade do token no reconcile. Sub-seam LGPD resolver titular_pseudo_id→fhir_patient_id permanece BLOCKED-by-design (sem store de reidentificação segura; fail-closed preservado). | Regra do kickoff: gap fechado OU explicitamente contabilizado com entrada rastreável; o script de reconcile lê decisions-log/review-queue como fonte de deferrals/credits | orchestrator-wave3 |
| 2026-07-03 | DL-0023 | HOTFIX direto no main (stop-the-line, precedente DL-0007): remoção da entrada manual `${grupo_humano}` de EXPRESSION_GROUP_RESOLUTIONS/EXPRESSION_DMN_SOURCE — trazida pelo #157 (registro bundled feito ANTES do #155 mergear) e proibida pelos testes-sanidade do #155 ("must stay resolved by the GENERAL flat-var mechanism"). Segunda instância da classe pairwise-green≠union-green NO MESMO ARQUIVO em uma sessão (#137×#144, depois #157×#155): CI de PR roda no merge-ref do momento do push e NÃO re-roda quando main avança antes do merge — lição operacional: re-validar lanes verificados-antigos contra main FRESCO antes de mergear quando main mudou no mesmo arquivo. Deleção provada segura localmente: 20/20 testes de candidate_groups verdes (o mecanismo geral do #155 resolve ${grupo_humano} identicamente — domínio nucleo-ans/regulatorio-ans/juridico-regulatorio). | Main vermelho bloqueia todos os merges; a deleção restaura o invariante do #155 sem perda funcional | orchestrator-wave3 |
| 2026-07-03 | DL-0022 | TAKEOVER Wave-2: o coordenador Wave-2 está INATIVO (diretiva do usuário); `orchestrator-wave3` assume TODA a conclusão Wave-2 + residue (#139 stdlib-scrub, lucas XHITL-4B/#140-fechado) — AMPLIA o escopo de DL-0021 (o `explicitly_not_mine` de fix/w2-*/#139/#140 fica SUPERSEDED; HANDOFF.yaml continua intocado até o close). Preservação executada ANTES de qualquer outra ação: snapshots commit+push de 8 worktrees sujos (arc c5c33ab, cancel 0971eb5 [10 arquivos, lane só-local], fc e959ff1, ia fbd1829, nip a8c6266, wapp 3b5364f, xobs56 4dfeb2e, logbridge b1c7dec). BOTTLENECK REGISTRADO: o classifier de auto-mode BLOQUEIA merge de PR autorado+verificado por agentes do próprio orquestrador (two-party review) — PRs verificados estacionam em READY-TO-MERGE (fila no HANDOFF-wave3.yaml) até merge humano ou regra de permissão; NENHUMA tentativa de contornar via push direto de branch (violaria a intenção do bloqueio). #141 é o primeiro estacionado (verifier PASS + integration pass). | Protocolo de preservação (memória Wave-1) manda snapshot antes de retomar lanes órfãs; único-orquestrador restaura os invariantes de DL-0015; fila explícita de merge mantém o gate humano audível | orchestrator-wave3 |
| 2026-07-03 | DL-0021 | Wave-3 (18 lows) + 4 itens de residue Wave-1 não-reivindicados (dispatcher-seam ×3 CONTAS-1/LGPD-2/PROG-1, NIP task_name label, sentinela competência ANS, paridade fail-safe de âncora) DELEGADOS a um SEGUNDO orquestrador paralelo (`orchestrator-wave3`), por diretiva do usuário — QUALIFICA DL-0016. Contrato de coordenação: propriedade de arquivos disjunta (fence em `docs/handoffs/HANDOFF-wave3.yaml`), hot files serializados first-merged-wins (nunca co-editados com fix/w2-* em voo), handoff SEPARADO (HANDOFF.yaml permanece exclusivo do coordenador Wave-2), gate de wave RESPEITADO (nenhum build Wave-3 antes de 41/41 gaps W2 reconciliados por script + main verde incl. lane real-engine; confirmado pelo usuário). O passo (6) "spawn Wave 3" do wave2_next_actions (#138) fica SUPERSEDED para esses itens; #139/#140/gauge user_task_pending permanecem do coordenador Wave-2. Residue Wave-1 NÃO é gateada — lançável quando file-disjunta dos diffs em voo (worker_runtime/service.py está em NENHUM diff aberto). | Dois orquestradores são legais só sob o contrato DL-0016; handoff separado evita contenção do HANDOFF.yaml (reescrito em #138); DL row + HANDOFF-wave3.yaml + comentário no PR #138 formam o fence anti-duplo-spawn com canal de leitura garantido | orchestrator-wave3 |
| 2026-07-02 | DL-0020 | **100% de cobertura BPMN DI obrigatória em todo `.bpmn`** — a convenção "DI adicionada na promoção a FINAL" está SUPERADA. Ferramenta de repo `src/maezo/platform/validation/bpmn_di.py` (gerador determinístico de auto-layout: ranking por caminho-mais-longo cycle-safe → grade centrada esquerda→direita → boundary events na borda do host, subProcess expandido com filhos dentro, arestas ortogonais) + subcomando CLI `python -m maezo.platform.validation.cli di [--check\|--write] [--force]` + regra de validação `check_di_coverage` (ERROR por flowNode sem BPMNShape / sequenceFlow sem BPMNEdge). É **preserva-se-completo**: a DI feita à mão de SP-OP-ESCALATION-001 (já 100%) é mantida byte-idêntica; só os 14 arquivos sem DI são regenerados. **Fase 1** (este PR): ferramenta + testes + regra, com o gate `bpmn.ENFORCE_DI_COVERAGE=False` (não regenera os 15 arquivos — outros PRs Wave-0 editam 3 deles). **Fase 2** (após merge dos PRs Wave-0): `di --write` em todos + trocar `ENFORCE_DI_COVERAGE=True`. | Diretriz vinculante do dono (2026-07-02): os `.bpmn` são abertos em viewers (Camunda Modeler) e a ausência de DI (1.073 elementos sem representação em 14 arquivos) impede revisão visual / human-in-the-loop. Auto-layout determinístico (mesma entrada → mesmos bytes) mantém o artefato versionável e a regeneração idempotente; a DI é ignorada pelo engine mas o output respeita a ordem XSD dos filhos (rootElements < BPMNDiagram; Bounds < BPMNLabel) para não quebrar o validador nem o deploy. Gate atrás de flag evita main vermelho enquanto os arquivos não foram regenerados (sequenciamento seguro, precedente DL-0014). | orchestrator/bpmn-di-coverage |
| 2026-06-14 | DL-0019 | ANS-SUBMIT retry/backoff modelado como subprocess embarcado `SUB_RetryEnvio` com DMN `ans_retry_policy` dirigindo contagem de tentativas + backoff exponencial (PT5M/PT30M/PT2H, DRAFT); worker `make_retransmit_handler` (topico `regulatorio.anssubmit.retransmit`) com o MESMO guard nao-repudio (`ERR_ANS_SUBMIT_NOT_HUMAN`) reusando a aprovacao humana — NUNCA re-autoriza; entrada via `ERR_ANS_PROTOCOLO_NACK` do submit; esgotamento via `ERR_ANS_RETRY_ESGOTADO` → `UT_TratarNack`. | ADR-0012 (DMN e a unica fonte de regra) — a politica de retry e regra ajustavel, logo vive em DMN, nao em literais de timer no BPMN; fecha a unica divergencia do audit `bpmn-process-completeness`; invariante HITL pre-filing preservada (guard no worker, nao no BPMN). | orchestrator/DOMAIN |
| 2026-06-14 | DL-0018 | Retenção de 5 anos do `audit_chain` (gap forense #6, ADR-0007) é NÃO-PARTICIONADA: tabela de 0002 mantida (`UNIQUE(prev_record_hash)` global, atômica) + `DELETE FROM audit_chain WHERE ts < cutoff` agendado (ISO-8601 lexicográfico) + retenção do tópico Kafka. Particionar foi REJEITADO. (run de auto-completion: 22 PRs #65–#86, todos os 37 gaps da auditoria forense resolvidos; relatório em `docs/reports/autonomous-completion-report.md`) | A lane real-engine PROVOU que particionar ENFRAQUECE a anti-fork (Seção 3 da auditoria): Postgres OBRIGA a UNIQUE de tabela particionada a incluir a chave de partição → `UNIQUE(prev_record_hash)` global degrada p/ `(prev_record_hash, ts)` + trigger cujo `IF EXISTS` NÃO é atômico sob READ COMMITTED → dois forks concorrentes ambos passam = fork silencioso (`test_concurrent_fork_*`: `assert 0==1`). 3 rounds confirmaram incompatibilidade: cast texto→timestamp/date é só STABLE (DateStyle), e coluna GENERATED não pode ser chave de partição. §7 (nunca enfraquecer guarda fail-closed) > escolha de implementação. Anti-fork volta à forma DB-atômica mais forte + retenção atendida | orchestrator |
| 2026-06-14 | DL-0017 | #55 conserta a lane de runtime (red main) com 2 correções + 1 exceção: (a) extensão pgvector criada em `public` (tipo/operador `vector` é DB-global, não por-tenant) — `0003 ... WITH SCHEMA public`, `env.py search_path "<tenant>", public`, `PostgresMemoryStore` search_path += public; (b) `PostgresAuditSink` usa asyncpg `setup=` (por-acquire), não `init=`; (c) EXCEÇÃO ADR-0011 forward-only: `0003_agent_memory` editada in-place | (a) sem isso o `register_vector` do asyncpg (search_path padrão) não acha o tipo, e o operador `<=>` (em public) fica invisível a um search_path só-do-tenant. (b) BUG REAL DE PRODUÇÃO achado por INSTRUMENTAÇÃO (3 teorias de migração estavam ERRADAS — a migração estava certa, audit_chain ERA criada): pools asyncpg rodam RESET ALL no release da conexão, zerando o `SET search_path` de sessão; com `init=` (só na criação), o 2º+ `emit()` numa conexão REUSADA pega o search_path padrão e o INSERT em audit_chain falha. (c) a 0003 nunca aplicou com sucesso em lugar algum (sempre vermelha, nada deployado), logo corrigir in-place não quebra deploy. Lição: instrumente bugs só-de-engine-real; use `setup=` p/ estado de sessão por-acquire | orchestrator/PLAT |
| 2026-06-13 | DL-0016 | Phase 3 BUILD DELEGADO a um SEGUNDO orquestrador PARALELO (QUALIFICA DL-0015) — brief em `docs/handoffs/phase3-orchestrator-brief.md`, estado em `HANDOFF-phase3.yaml` | Mudança de fator: pressão de janela de contexto do orquestrador de runtime (>85%). Offload de uma track grande p/ um contexto FRESCO reduz o risco de uma compaction disruptiva e deixa runtime (W-R2/W-R3/Track-S code) e domínio (Phase 3) progredirem em paralelo sem consumir o contexto escasso do runtime. As objeções de DL-0015 são endereçadas por um CONTRATO DE COORDENAÇÃO: partição de propriedade de arquivos; arquivos COMPARTILHADOS (a2a, process_allowlist, topic_registry, autonomy) coordenados e NÃO co-editados; dependência A2A resolvida por SEQUENCIAMENTO (W-D agentes esperam W-R2/R9 mergear; W0/W-A/W-B/W-C não precisam de runtime); handoff SEPARADO (HANDOFF-phase3.yaml) evita contenção do single-HANDOFF; worktrees isolados (files/gates/caches). Runtime-first (DL-0014) preservado: Phase 3 build não bloqueia nem é bloqueado exceto no seam A2A | orchestrator |
| 2026-06-13 | DL-0015 | Phase 3 NÃO delegado a um orquestrador PARALELO; tratado por ESTE orquestrador, sequenciado após Track R, com overlap de fundações disjuntas em worktrees | Dois orquestradores paralelos quebram os invariantes de orquestrador-único (1 HANDOFF/fonte-da-verdade, 1 autoridade dono-de-arquivo, 1 fila de merge/stop-the-line) e não têm protocolo de coordenação; colisões reais de arquivo (a2a/dispatcher: R9 ∩ delegações dos agentes Phase 3; process_allowlist.py; config/topic_registry.yaml; audit.py↔custody.py) e o A2A é dependência DURA — agentes Phase 3 constroem sobre a assembly de produção de W-R2/R9 (runtime-first, DL-0014). Paralelismo SEGURO = worktrees isolados (files/gates/caches/branch separados) sob UM orquestrador: agora W-R1 (PR #48) + Track C (worktree) + Phase 3 foundations (worktree) em paralelo | orchestrator |
| 2026-06-13 | DL-0014 | Plano de plataforma entregue (`docs/reports/platform-completion-plan.md`); sequenciamento RUNTIME-FIRST: Track R (runtime-wiring) antes de mais Phase 3 | Uma sweep forense de 6 domínios (read-only, cross-checada na working tree @b2641f5) achou a tese central: a plataforma é **biblioteca-com-testes, não roda como serviço** — 0 `__main__` em src, sem Dockerfile de app, 4 Deployments Helm apontam p/ módulos sem `__main__`/inexistentes (crash-loop), harness sem health server, audit hash-chain + idempotência A2A em memória (forka/duplica no restart). Autorar mais Phase 3 sobre um runtime que não executa os 9 processos + 5 agentes atuais só acumula BPMN nunca-executado. Logo o trabalho de maior alavancagem é o runtime-wiring (R1+R2+R3+R5), 100% buildable sem AWS. ~57 gaps consolidados (de ~107 raw) em 5 tracks (R/C/3/S/P) | orchestrator |
| 2026-06-13 | DL-0013 | ADR-0018 codifica o padrão estrutural no-denial de 5 partes como VINCULANTE para todo SP-OP negativa-like | Operacionaliza ADR-0005/0008/0012 numa invariante por construção (tipos sem variante adversa + DMN sem saída de negativa + fail-safe a User Task por allowlist fechada + worker guard ERR_*_NOT_HUMAN + teste de integração na história do engine real). Provado + CI-enforced em AUTH-001/CONTAS-001; replicado nos 5 bodies de PR #38; mandato p/ futuros (SP-OP-FRAUDE, SP-OP-INADIMPLENCIA) | ARCH/DOCS |
| 2026-06-13 | DL-0012 | ADR-0017 operacionaliza a metade de REDE da ADR-0006 (egress PHI por CIDR IP-fixado, fail-closed) | Uma revisão adversarial pegou um egress `0.0.0.0/0` que tornava a NetworkPolicy PHI um no-op (permitia a internet inteira incl. LLM cloud geral). Chart `maezo-tenant` fail-close (helm `fail`) em CIDR PHI ausente/`0.0.0.0/0`; intra-namespace escopado ao gateway (quebra relay PHI→geral→cloud); securityZone cross-validado contra a definição mergeada. FQDN exige egress proxy/Cilium toFQDNs (FUTURO, não entregue) | ARCH/DOCS |
| 2026-06-13 | DL-0011 | `src/maezo/platform/validation/bpmn.py` excluido do `ruff format` (não do `ruff check`) via `[tool.ruff.format].exclude` | Divergencia cross-platform/não-determinística do `ruff format` 0.15.17 (pinado): o runner CI manylinux intermitentemente quer reformatar um literal de coleção que o macOS arm64 (e outras execuções CI, ex. main @1c63714 verde) consideram já formatado — mesmos bytes, mesma versão, veredito diferente. Bloqueava `make lint` de TODO PR (gate tree-wide). Stop-the-line (§4, precedente DL-0007); lint (E/F/I/N/W/UP/B/SIM/ASYNC) continua cobrindo o arquivo. Revisitar quando o ruff resolver | orchestrator |
| 2026-06-13 | DL-0010 | ADR-0016 adicionado (process_key allowlist + invariante PHI no ToolRegistry) | Decisão de arquitetura de segurança com enforcement estrutural; justifica ADR proprio (não apenas DL): cria nova camada de recusa entre PEP e handler, e torna ADR-0006 uma invariante verificável em código (PR #19) | ARCH/DOCS |
| 2026-06-13 | DL-0009 | ADR-0015 formaliza runtime A2A de PR #17 (complementa ADR-0003) | ADR-0003 escolheu protocolo; PR #17 tomou decisoes concretas de estrutura de dados, contrato PHI no envelope e injecao de handlers que precisam de ADR proprio | ARCH/DOCS |
| 2026-06-13 | DL-0008 | ADR-0014 converte recomendacao W7 (DL-W7-001) em decisao aceita: AMP/AMG para staging/prod; Prometheus/Grafana in-cluster para dev | Avaliacao de custo W7 (~$30–50/mês Phase 0-1 < limiar §2-bis); alinhamento com modulo observability-stack da amh-data-platform; PR #21 ja implementou | ARCH/DOCS |
| 2026-06-12 | DL-0007 | Hotfix direto no main: regex de filename BPMN aceita underscore no Titulo | Main vermelho (validator W1 × convenção usada pelo W5 nos 3 BPMN mergeados); stop-the-line §4 | orchestrator |
| 2026-06-12 | DL-0006 | Imagem CIB Seven pinada em 2.1.0 | Tag 2.1.3 dos ADRs não existe no Docker Hub; 2.1.0 é a mais próxima na linha 2.1.x; revisitar quando publicarem | orchestrator |
| 2026-06-12 | DL-0005 | PEP: L0 `hard` → DENY; L0 não-hard → REQUIRE_HUMAN (dossiê); L1 → REQUIRE_HUMAN; L2/L3 → ALLOW | Mapeamento ADR-0005/0008 proposto pelo W2 e aprovado em review ARCH (PR #1) | orchestrator |
| 2026-06-12 | DL-0004 | PR #2 (validation CLI) mergeado com jobs integration/evals vermelhos | Falhas pré-existentes de infra (imagem `cibseven/cibseven:2.1.3` inexistente; job evals sem harness) já no escopo do W7; quality+artifact-validation verdes | orchestrator |
| 2026-06-12 | DL-0003 | Updates de HANDOFF.yaml/decisions-log.md vão por push direto do orquestrador | Metadados de estado do swarm, não código; PR para cada update geraria churn sem revisor | orchestrator |
| 2026-06-12 | DL-0002 | Sem infra CDC neste repo: consumimos os tópicos Debezium da amh-data-platform (CDC Tasy já em produção lá: Debezium/ECS Fargate + MSK Serverless) | §2-bis (precedência da Plataforma de Dados); ADR-0013 formaliza (W3) | orchestrator |
| 2026-06-12 | DL-0001 | Log inicializado no kickoff do swarm Phase 0 | §9 do prompt de execução | orchestrator |
