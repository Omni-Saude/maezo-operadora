# Finite D7 operational consumer handoff

This is a distinct-author implementation mandate following SAME qualification of the actual historical observation. It does not activate a ledger record, provide a current proof, or permit archive-only acceptance. Implement the concrete D7 lane; do not leave a permanent UNRESOLVED stub or replace the accepted contracts with a broad skip.

1. Add one separate finite successor descriptor for D7unit802, using the exact actual pins in HISTORICAL-DESCRIPTOR.json and this review's final REPORT/MANIFEST. Keep original babe CATALOG/PINS, existing INVALID/VALID catalogues and their original receipts unchanged. Do not add D6migration to the unit catalogue. The D7 tuple is source802, physical rowd23cc70, test/declaration4ba, own-lockc3be, actual recipe1b40, selected/passed159. The row date2026-09-08 permits fixed result-lines only (cutoff2026-09-06); declaration equals old source hash and differs from its eligible canonical recipe.

2. Authenticate complete outer and inner archive membership/custody and the separate SAME source/actual review pins before trusting any operative field. Explicitly recognize outer successor-async schema, inner async schema, recipe discriminator, actual producer241e and entrypointa814, catalogue33676, loader0c9f and original helper25a4/runner0281/checker8ac5/guard2ed3 dependency closure. The async receipt is not original babe v1. No argv/schema normalization, old-producer relabeling or caller-supplied approval/runtime trust flag. Bind the new validator transitively into the operational producer capsule, candidate/source equality checks, dependency/diff closure and result provenance. Resolve source pins explicitly against the integrated candidate; do not silently update original pins to make imports pass.

3. Retain literal local runtime refusal. If the CI/archive consumer needs portable readback, apply the already accepted finite archived-producer identity pattern only to this exact new frozen producer/packet/runtime tuple, with source review of the new adapter. Never reopen record-selected host executables/plugin paths as trust, disable native foreign-runtime refusal, or allow a portable archive check to count as fresh execution. Actual local executions must continue to use their independently selected runtime and fresh own-lock environments.

4. Each selected D7 relation must still execute an actual NEW historical capture and an actual NEW current whole-file proof. Use the explicit async recipe and actual own-venv plugin provenance on both applicable sides; no reuse of this archive as the fresh historical run. The current proof must be on the actual integrated candidate and the exact same test path; current result/count/source/recipe and row fields come from real execution, never the historical219 narrative. Preserve the accepted ordered refusal of unrelated/renamed/deleted/replaced current or prior-corrective test paths before replay; current FAIL cannot be rescued by this archive. Preserve all source/row/test/lock/config/graph/collision/cycle/physical-occurrence, temporal/freshness, candidate immutability, current corrective/prior corrective and evidence-diff-closure checks.

5. Preserve the closed v2 invalid-declaration grammar and narrow declared-test-source-sha256 reason. Preserve v1 same-lock equality and PFV-only valid own-lock v3; D7 is not a valid-history exception. All original physical rows and the full qualified ledger prefix plus every later ROOT row remain immutable. Only append a fully bound correction/record after its real current proof exists and the accepted operational relation verifies. Include the complete committed relation-owned outer/inner evidence and accepted review closure; no fabricated hashes or acceptance markers during preparation.

6. Prove the finite consumer with independent positive fresh-old/fresh-current controls and negatives for swapped history/current, missing/forged/resealed outer or inner proof, wrong schema/producer/catalogue/report/source/row/lock/plugin, current failing or unrelated same-file bindings, missing review closure and changed candidate/source pins. Missing actual fresh execution must produce no verification credit. Preserve old v1/v2/v3 controls and private CI proof retention/failure behavior. Freeze the source delta for SAME review, then retain actual execution results and request SAME operational acceptance review on the exact integrated candidate.

D6 migration service-aware acceptance remains a separate owned-lifecycle implementation. This historical-only D7 approval closes none of that work, no current D7 relation and no whole-goal gate.
