# SAME D7-ASYNC-R1/R2/R3 source delta

**Qualified APPROVE** for exact `63f1f1ca33e3887a545fc85ec27673feb44210d0`, tree `1ccc563fef4fdd102d968ee7c4ba49a7f3447579`, directly over581dcd6e37befe7a71c085fe8b89a1662bf82038. The three bounded async repair mandates are satisfied. Approval covers the explicit async successor/source closure and the pinned ROOT capture/readback handoff; it grants no actual historical/current result, ledger acceptance, service evidence, operational promotion or final GK.

Independent clean worktree: `/Users/familia/code/maezo-completion-wt/ledger-d7-async-successor-verify-63f1`. No implementation/source repair, real historical execution, service/engine, full unit/global ledger, GitHub or nested agent was used.

## Custody, identity and unchanged boundaries

Independently rehashed author REPORT `80d95c8a7a401e87c27115c2c6c66dff22793fbe6186f9f82ff2f5fd7c76067f`, MANIFEST `72e766d4034df9c122a7c8b03feee60364cd2e7ac922f5508dabc4ff66ad1ea2`, all6,520 regular entries/35,102,760 bytes. Special fixtures were not followed. Original author failures and repeated runs remain in that immutable packet.

Exactly three changed paths: new async producer `scripts/dev/historical_async_recipe.py` SHA `241e0c9a9f636abe50f6765f148d0668c78ea3f26ab90f91725368434f83d4af`; catalogue entrypoint SHA `a814ab8dacce1e6e5541af01e601aaa183d6e6917da014145a46d47ab54c9be4`; new tests SHA `abdfeba3f71b52f19c91628f5c7e4d4b022a97ad8c78a75598053cfbb68cc5a8`. All other versioned paths are unchanged. Independent byte/Git checks preserve nine protected helper/loader/migration/operational/lock/ledger paths, the closed catalogue AST, and all12 legacy catalogue functions/classes except intentionally changed CLI routing. Full base ledger remains SHA7182fb480d56016a83c9bde57ad7cabe4badd8a9bc1b6555a45e0ced90c84ac3; later ROOT rows also require preservation on integration.

Original literal helper25a41249, runner02810ce8, checker8ac5085b and loader0c9f10f5 remain exact. This repair does not change original CATALOG/PINS, old receipt rules, native foreign-runtime refusal, the migration lifetime adapter or any v1/v2/v3 acceptance rule. The new module is descriptor-read/nonfollowing/bounded and hash-pinned before compilation by the existing authenticated entrypoint path. Its actual __file__ identifies its own new source.

## R1: explicit async recipe and complete native validation

The new inner schema is maezo-historical-async-recipe-proof/v1; the outer schema is maezo-historical-unit-successor-async-capture/v1; recipe discriminator whole-file-verbose-explicit-asyncio/v1. The inner source/tooling.helper is the actual async producer241e; original_helper is babe25a4 as a dependency. The outer identifies current entrypointa814 and inner producer241e separately. Original result-lines-fixed-v1 is only the result extraction algorithm, not the producer identity.

The producer verifies disabled autoload and appends only the fixed `-p pytest_asyncio.plugin` pair to the guarded whole-file verbose recipe. There is no original monkeypatch, selector/plugin/environment CLI extension, argv normalization or old-v1 receipt relabeling. New capture and validation routines use the literal old primitives rather than calling old capture_source/validate_receipt. Static normalized-routine comparison (analysis only, never executed) and direct source review show original checks retained; differences are explicit async recipe/schema/tooling identity and additional validation.

The new full validator enforces finite file/directory membership, strict nested keys/types, complete Git blob/source maps, exact row/declaration/config, actual producer/dependency identity, original guard bytes, every command/raw split stream/hash and quiescent-success status, exact async argv/cwd, before/after inventory, independently selected runtime/uv, exact locked preparation, plugin provenance, phase/result recomputation, observation-only scope, cleanup and manifest. Additional checks bind command datetime shape/order and positive PGID, exact clone/checkout/uv argv, and no pending cleanup groups. Plugin inventory must match the own source lock and its actual archived __init__.py/plugin.py paths must reside in the same .venv distribution. No plugin file named by a receipt is opened for host trust.

This preserves the trust boundary: native readback recomputes consistency against independently selected sources/runtime; an asserted envelope is not approval. Future portable/operational consumption still requires its separate reviewed finite descriptor and actual custody pins.

## R2: independent controls and disclosed timing failure

One complete204-test invocation returned **203 passed,1 failed,1 warning in124.25s**. All61 new async controls passed, as did142 existing controls. The sole failure was the unchanged test_real_fifo_finishes_without_timeout: its one-second subprocess timeout covers helper import plus FIFO loading. The original timed-out child's precise subphase was not instrumented, so this report does not assign an unobserved cause.

The exact unchanged test then passed in isolation under its unchanged one-second limit (**1 passed**). A separate timing diagnostic measured0.423753s helper import and0.000354s FIFO refusal with nonregular/private-evidence rejection. No source, fixture assertion or deadline was relaxed. Thus all204 distinct controls have passing observations across invocations, but **this is not a single204-pass run**. The initial RED, XML/raw command, exact rerun and diagnostic remain preserved. No new source defect was identified in this unchanged FIFO path. The warning is the known asyncio_mode configuration option with plugin autoload disabled in the outer control suite.

Independent tiny packet inspection confirms sync→entered→awaited→completed,2 selected/six passing phases, locked own-venv plugin provenance, distinct inner/outer identities, native new readback and source cleanup. The original v1 validator refuses this new schema. Five actual production-path tiny failures—missing/disabled explicit plugin, unavailable distribution, shadow plugin, async assertion failure—return nonzero, retain failure evidence, emit no success receipt/envelope and clean scratch/pending groups. No-plugin mutants reach only sync; the assertion-failure case reaches entered/awaited but not completed. Resealed argv/plugin/inventory/source/row/types/phase/guard/cleanup/runtime/outer-envelope forgeries and nonregular source replacements also pass their refusal controls.

Ruff check/format and strict mypy of both production modules pass; exact isolated preflight-async returns0 under the documented ROOT runtime/env. The author204 count is61+143 across invocations, and its three native old readbacks are author evidence only in this delta. Independently here, all members of the original581 review and its corrective diagnosis were rehashed unchanged, along with every original PFV/D6unit/D6migration manifest member and all28 failed581 capture files. Prior native-readback approvals remain valid for unchanged originals; no new historical execution is implied.

Two local reviewer summary/timing harness errors are disclosed in reviewer-harness-notes.txt: a diagnostic text substitution caused SyntaxError before import, and an artifact-summary glob missed pytest's truncated fixture names before writing output. Their original diagnostics are retained; corrected diagnostic/summary passed. Neither altered candidate source nor reran historical D7.

## R3: corrected closure and exact ROOT handoff

SOURCE-CLOSURE.md qualifies the unchanged source802 test/dependency closure with the new explicit plugin path. The old581 ordinary-autoload statement and capture-readiness remain withdrawn by REPORT51125b65dbcd1dbc2c0b7842bae06aeb2de6ded234bf3d11a3901f12cb1fb966; this approval does not revive legacy capture mode. The source catalogue still fixes rowd23cc70, source802, test/declaration4ba and own-lockc3be. The159/219 narrative counts are not expectations.

ROOT may use the frozen ROOT-NEXT-COMMAND.json **preflight-async, capture-async and readback-async** modes on exact63f1. Independently recheck clean HEAD/tree, all three source hashes and original literal dependency pins, this review/report/manifest and the command specification. Use Python SHAa395f264e5612a2819662ed3e37fd30d39ed61179b98e5f86c3c783a008d8623 and uv SHA0e71bad1f36bc9762cdecef1932f68ea3db541e45495c60dd474e1c860e21edf, both rehashed here, with exactly the documented closed environment, umask077 and DEVNULL. Create a new nonsymlink private0700 parent with nonexistent target under guarded-d7-explicit-async-catalog-v1. Use a new bounded ROOT supervisor preserving split raw capture/readback streams, argv/time/pid/returncode and quiescence; keep failed581 supervisor/output immutable. Do not run readback or mint acceptance after capture failure.

After actual ROOT execution, freeze outer/inner recipe, source, plugin, runtime and cleanup evidence and request SAME actual delta. Any future source change must preserve original identities and qualify its actual producer. Actual historical classification, fresh historical plus fresh current same-file acceptance, operational catalogue/validator integration and D6 service-aware acceptance remain required separate work. No old failure is converted to a business pass, and no acceptance marker/record is authorized by this source review alone.
