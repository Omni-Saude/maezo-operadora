"""Integration daemons (T2.6-EB3) — sibling package to `platform.webhooks`.

Today: `notifications_bridge` — the Kafka consumer entry point for `NotificationBridge`
(`deployment-bridge.yaml`'s `python -m maezo.platform.integrations.notifications_bridge`).
"""

from __future__ import annotations
