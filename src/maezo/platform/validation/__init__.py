"""MAEZO Platform Validation — BPMN, DMN, policies, and agent-definition validators.

Used by the CI pipeline (make validate-artifacts) to assert that all
compliance artifacts are well-formed, referenced, and consistent.
"""
