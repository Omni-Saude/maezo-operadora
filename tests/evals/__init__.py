"""Behavioral evals (T3.2, ADR-0009 golden-dataset promotion gate).

Package init only — mirrors `tests/support/__init__.py`'s "importable, never a fixture magic
module" posture. See `tests/evals/README.md` for the two-tier model (Tier A deterministic
replay / Tier B live-LLM), the golden JSON schema, and how to add a new eval.
"""
